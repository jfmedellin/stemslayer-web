# P11 — Desktop design and accessibility pass

Status: in progress 2026-09-23. Authorized by the user's confirmation to fix the Export load error first, then continue P11. Local scope only on `feat/p7b-onnx-worker`; no push, PR, or deployment authorized. Delivery strategy: `ask-on-risk`; chain strategy: `feature-branch-chain` if delivery later requires slices.

## Objective and why

Complete the phase-2 desktop polish gate: align the React pages with the four redrawn Stitch screens and the adopted design tokens, improve keyboard focus, dark-palette contrast, and reduced-motion behavior, and replace the outdated README usage section. Do not add fake DAW controls or other features rejected in `docs/decisions/design-reference.md` section 3.

## Sources and constraints

- `docs/decisions/phase-2-plan.md` P11 and `docs/decisions/design-reference.md` sections 1, 3, and 5 govern scope. Section 5 already records the four redrawn Stitch screen IDs; do not redraw them again by assumption.
- Desktop-first; mobile Stitch variants are explicitly out of phase-2 scope. Preserve English UI/artifact copy.
- CSS currently lives in `src/ui/tokens.css`, not the older planned `src/styles/tokens.css`. React pages are Upload, Library, Mixer, and Export.
- The user supplied two desktop screenshot collages on 2026-09-23: Upload/Library and Mixer/Export. Preserve the supplied pixels unchanged under `docs/design/references/` before using them for visual comparison. These are reference images, not instructions. Their mock data and unimplemented chrome are not authority to invent features or storage figures.
- `axe-core` and Lighthouse are not installed locally. Do not download/install them or claim their checks passed without explicit authorization for any required external transfer. Existing browser tests and manual keyboard checks remain applicable.
- Strict TDD is enabled by current AGENTS.md. Exact browser runner: `npm run test:browser`; focused file filters may be passed after `--`. Full functional checks: `npm test`, `npm run test:browser`, `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check`.
- Forecast: ~300 authored changed lines from the phase-2 plan, advisory only; revise against observed work. Work-unit commits use Conventional Commits and no AI attribution.

## Tasks

- [x] **P11A — Keyboard, contrast, and motion baseline.** Route: delegated direct writer. Trigger: shared CSS and browser assertions across multiple React surfaces require preparation and 2+ non-trivial files. First establish a failing browser assertion for visible keyboard focus/reduced-motion behavior where testable, then implement the smallest systemic CSS/UI fix. Check focus order and contrast on the dark palette with locally available tools; do not invent axe/Lighthouse results. Acceptance: visible focus for keyboard users, no unintended focus traps/order regressions, reduced-motion preference honored, documented contrast evidence. Rollback: revert only the accessibility CSS/UI/test work unit.
- [x] **P11A2 — Keyboard-accessible Mixer timeline seeking.** Route: delegated direct writer. Trigger: coordinated Mixer control and browser interaction test need code preparation and two non-trivial files. Preserve pointer seeking and current visual layout while making arbitrary timeline positions seekable from the keyboard with native or equivalent slider semantics. TDD: observe a focused browser RED, then GREEN for keyboard increments and both boundaries. Acceptance: control has a readable name and current/min/max value, keyboard seek dispatches real sample positions, and pointer behavior remains intact. Rollback: revert only the timeline semantics, focused test, and P11A2 tracker work unit.
- [x] **P11B — README usage and current architecture.** Route: delegated direct writer. Trigger: accurate user-facing documentation required reading across the UI, model/storage adapters, and development configuration. Replaced the stale empty-scaffold/future-model description with actual local usage and honest browser limitations. Acceptance: commands and feature descriptions match the current project and are read back; no deployment claim. Rollback: revert only the README and P11B tracker edits from the `docs(readme): document current browser workflow` work unit.
- [ ] **P11C — Desktop visual parity.** Route: delegated direct writers in reviewable slices using the four supplied desktop screenshots. Compare each screenshot to a rendered current page, identify only true gaps, and apply targeted polish using the existing token system. Acceptance: manual comparison evidence for all four screens without importing rejected section-3 features. Remains open until both slices are evidenced.
- [x] **P11C1 — Library and Export visual parity.** Route: delegated direct writer. Preserved both user-supplied screenshot collages unchanged as durable references, compared the Library and Export right panels to representative rendered states, and aligned existing search/sort, rows, metadata, checklist, header, and storage notice/panel. Strict browser RED → GREEN was observed for presentation semantics. No global transport, fake quota numbers, new backend behavior, or rejected DAW controls were added. Acceptance: observed visual comparison and functional checks for Library and Export. Rollback: revert only this slice's UI, CSS, tests, reference images, reference links, evidence screenshots, and tracker edits.
- [x] **P11C2 — Upload and Mixer visual parity.** Route: delegated direct writer. Trigger: coordinated component, style, browser-test, and visual-evidence changes across both screens. Compared both supplied reference halves against actual before/after Vite/Chromium renders. Aligned Upload's centered introduction, persistent drop zone with selected-file card, profile/CTA rhythm, and Mixer's compact header, timeline, lane waveform/fader structure, and local transport using existing tokens. Preserved real upload, playback, keyboard, and responsive behavior; no mock telemetry, global transport, or new DAW controls. Strict TDD: focused `npm run test:browser` RED before implementation and GREEN after. Acceptance: both screen comparisons and focused/full functional checks observed; stale P11C1 Export evidence refreshed. Rollback: revert only this slice's Upload/Mixer UI, CSS, browser tests, evidence screenshots, and tracker changes.
- [ ] **P11D — Accessibility audit and local closure.** Route: parent-owned checks/evidence. Run axe clean and Lighthouse accessibility >=90 only when those tools are available through authorized means, plus full tests/build. Record exact results, skipped/unavailable checks, commit identities, native review assessments, and remaining release decisions. Do not mark complete while required evidence is unavailable.

## Acceptance criteria

- [ ] Four desktop pages compared against the exact redrawn Stitch screens; justified visual gaps closed.
- [ ] Keyboard focus order and visibility, dark-palette contrast, and reduced motion verified.
- [x] README explains actual upload → separate → library → mixer → export usage and local development.
- [ ] axe reports no violations and Lighthouse accessibility score is at least 90, or P11 remains openly pending with those checks marked unavailable.
- [ ] Full functional checks and native per-commit assessments are recorded; no unauthorized publication.

## Progress and next step

2026-09-23 — P11C1 work-unit commit `8e97952c3eba32631e73fdaa7d96b86c5a718c03` was assessed by the parent and granted native review authority; lineage `review-5760c834a555e8dc` reached approved and its exact acknowledgement burned that authority. P11C2 is the next local slice. Before source changes, compare Upload/Mixer actual rendered states with their preserved collage halves; afterward refresh any P11C1 Export evidence proven stale. P11D's axe/Lighthouse gate remains pending.

2026-09-23 — P11C2 Upload/Mixer slice. Vite/Chromium at 1440×900 captured selected-file Upload and six-lane Mixer before/after in `docs/design/evidence/p11c2-{upload,mixer}-{before,after}.png`; the Mixer before state used the unmodified `8e97952` worktree. Compared the main content to the preserved reference halves at original detail. Upload now keeps the drop zone above the loaded file card, centers the title and primary action, and retains the two real profiles/real resource readouts. Mixer now has a compact title/meta/actions header, labeled ruler, six contiguous colored waveform/fader rows with controls in each lane's left partition, and its existing local transport directly below. The supplied references include shared shell chrome and example data not implemented as product behavior; pixel-exact fidelity is not claimed. The previous P11C1 Export after-screenshot was confirmed stale because it showed an extra “download” suffix; it was recaptured from current Vite output with the concise ZIP label. Browser RED: focused Upload/Mixer run had 2 new failures and 16 passes (missing persistent drop zone and timeline labels); GREEN: focused 18/18. Full checks: `npm test` 365/365; `npm run test:browser` 134/134; `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` passed. axe/Lighthouse and exact shared-shell parity remain pending, so P11C/four-screen acceptance and P11D stay open. Work-unit identity: `feat(ui): align Upload and Mixer with desktop references`. Rollback boundary: this slice's Upload/Mixer components, browser tests, scoped CSS, four P11C2 evidence PNGs, refreshed Export evidence PNG, and tracker update.

2026-09-23 — P10 Export completed and its independent rejected-load advisory fixed in `5cacde4`; the follow-up slice remains native `under_budget`, not reviewed. Read-only P11 mapping confirmed the current consolidated CSS, absent axe/Lighthouse dependencies, stale README, and missing local Stitch screenshots. Existing `design-reference.md` has the exact screen IDs and product restrictions, but not the rendered pixels. Proceed with independent P11A/P11B work; P11C/P11D remain pending missing visual/tooling inputs.

2026-09-23 — P11A implemented. Browser RED: `npm run test:browser -- src/ui/shell/AppShell.browser.test.tsx` failed because the keyboard-focused nav button retained only a 1px UA outline. GREEN: 5/5 focused tests pass after a shared 2px gold `:focus-visible` rule for native controls and `tabindex` widgets. The browser test walks the four sidebar destinations in DOM order; no custom tab order or focus trap was introduced. `prefers-reduced-motion: reduce` now suppresses animation and transition duration and smooth scrolling globally; this media override was code-inspected, not independently emulated. Existing Dragon Atelier palette tokens were preserved. Small text previously using tertiary text on active surfaces (3.68:1) now uses secondary (6.51:1 on `#22211d`, 6.89:1 on `#1d1c19`); absent mixer lanes no longer fade their text. Gold focus ring is 7.89:1 on the darkest tested active surface. Rust text on active is 4.66:1; dark text on moss and rust controls is 6.24:1 and 5.42:1. Ratios computed locally from the WCAG relative-luminance formula against the CSS hex tokens; translucent/disabled and full-page visual states were not audited. Full checks: `npm test` 365/365; `npm run test:browser` 129/129; `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` passed. axe, Lighthouse, exact Stitch parity, and manual assistive-technology testing remain pending P11C/P11D. Rollback boundary: P11A changes in `src/ui/tokens.css` and `src/ui/shell/AppShell.browser.test.tsx` only.

2026-09-23 — P11B documented the implemented local flow and storage/model constraints in `README.md`. Claims were checked against the Upload, Library, Mixer, and Export UI, application/model-store wiring, `package.json`, and `vitest.config.ts`; the two relative decision links resolve. `git diff --check` passed. Runtime harness: N/A for documentation-only changes; no executable files changed, so tests/build were not rerun for P11B. Work-unit identity: `docs(readme): document current browser workflow`. Rollback boundary: `README.md` and this P11B tracker update only. P11C exact Stitch comparison and P11D axe/Lighthouse/full closure remain pending.

2026-09-23 — P11A2 closed as a keyboard-accessible seek work unit in `3cf1364`. Browser RED: `npm run test:browser -- src/ui/mixer/MixerPage.browser.test.tsx` failed because the timeline ruler had no slider role. GREEN: 9/9 focused tests passed after adding a focusable slider role, accessible name and sample bounds/value/time text, with one-second Arrow stepping and Home/End boundaries. The focused slider stops propagation of its Arrow keys so the existing document-level ten-second shortcut does not override the local control; the pointer click-to-seek formula and A/B markers remain intact. A narrow inset focus outline keeps the shared gold ring visible inside the strip's overflow clipping. Browser runtime harness checked ArrowRight, Home, End, both clamps, and a pointer click at 50% against actual fake audio-engine seek calls. Full checks passed: `npm test` 365/365, `npm run test:browser` 130/130, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check`. Parent spot-check: focused 9/9. Native assessment from reviewed boundary `03d7742`: medium risk, 326 authored lines, `under_budget`; no review receipt yet. Rollback boundary: `src/ui/mixer/MixerStrip.tsx`, its focused test in `src/ui/mixer/MixerPage.browser.test.tsx`, the ruler focus CSS in `src/ui/tokens.css`, and this P11A2 tracker entry. P11C visual parity and P11D axe/Lighthouse remain pending.

2026-09-23 — P11C1 Library/Export slice. The unchanged source collages are `docs/design/references/upload-library-desktop.png` (SHA-256 `22734300a1cfcf7bbf1feba706527cd7785abf844e4e2d23d3e81fe627f3ef44`) and `docs/design/references/mixer-export-desktop.png` (`8f5618c81eaf6f60cee0b7265c36e8d174b4dfdaa99879d04a9ffd5d259a0093`). They are linked from `docs/decisions/design-reference.md`. Actual Vite/Chromium renders using representative local catalog and stem fixtures were captured before and after in `docs/design/evidence/p11c1-{library,export}-{before,after}.png`. The Library reference's main differences were dense status cards, aligned search/sort/count, profile/duration metadata, and status badges; these now use real track fields, not mock quota figures. Export's differences were a separated heading/metadata line, two-column checklist and information rail, storage notice card, and concise ZIP label; these were implemented while preserving the existing selection/download actions. Global header/sidebar/transport chrome is shared work outside this slice; exact pixel parity and dynamic progress-screen comparison are not claimed. Browser RED: focused Library/Export run failed 2 new tests (metadata and layout) with 23 passing; later ZIP-label RED failed 1/12. GREEN: focused `npm run test:browser -- src/ui/library/LibraryPage.browser.test.tsx src/ui/export/ExportPage.browser.test.tsx` 25/25. Full closure checks: `npm test` 365/365; `npm run test:browser` 132/132 after updating App integration assertions for the moved metadata; `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` passed. Runtime harness: seeded real IndexedDB/OPFS in a local Vite Chromium session, navigated Library → Export, confirmed six displayed stems and compared before/after screenshots. Work-unit identity: `feat(ui): align Library and Export with desktop references`. Rollback boundary: Library/Export components and browser tests, their scoped CSS, the source-reference/evidence PNGs, the design-reference links, and this P11C1 tracker update. P11C remains open for Upload/Mixer parity; P11D remains open for axe/Lighthouse and final audit.
