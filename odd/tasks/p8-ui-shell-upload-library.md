# P8 — UI: shell, upload page, library page

Status: complete. P8A/P8B closed with full strict-TDD evidence; native review could not be admitted (reproducible upstream tooling defect, reported and tracked — see P8C evidence), so P8 closes on manual verification, same precedent as P7A-02B. Delivery strategy: `ask-on-risk`; chain strategy: `feature-branch-chain` (continuing P7a/P7b's convention). Developed locally on `feat/p7b-onnx-worker` (has P1..P7b); publication base decided at closure, same reasoning as P7a/P7b.

## Objective

Build the real application shell (left navigation: Upload / Library / Mixer / Export, storage meter) and its first two destinations: the Upload page (drop zone, file card, profile choice, "before you start" strip, primary action wired to `addToLibrary`) and the Library page (dense track list with per-row status, search, sort, cancel/retry/remove wired to `separate`/`retryTrack`/`removeTrack`). Replaces the P1 placeholder shell in `src/ui/App.tsx` with the real product surface.

## Problem / why

P1..P7b built every port and use case this needs (`addToLibrary`, `separate`/`SeparationQueue`, `retryTrack`, `removeTrack`, `runStartupSweeps`, `CatalogPort`, `QuotaPort`, `ModelStorePort`) but there is still no UI calling any of them — `src/ui/App.tsx` is the untouched P1 "foundation ready" placeholder. P8 is the phase that makes the app usable end to end for its first two destinations, matching `docs/decisions/phase-2-plan.md`'s P8a/P8b split and the redrawn Stitch design reference (`docs/decisions/design-reference.md` section 5, screens `1d2398e695504d59be8edca762a85bcd` Upload and `45c441aa237c4c448a7fa020c5320400` Library, fetched and read directly for this document).

## Scope

- Expand `src/ui/tokens.css` from the P1 partial token set to the full Dragon Atelier system in `design-reference.md` section 1: stem colors including the two Rock guitar tints (centre `#c8b38d`, sides `#d6c5a0`, other/olive `#9fa685`), rust `#c4746e`, the full type scale (headline-xl/lg/md, body-lg/md/sm, telemetry-lg/md/sm, label-caps 9px mono uppercase), elevation/overlay rules, meter segment colors.
- App shell: left nav with four destinations (Upload, Library, Mixer, Export — Mixer/Export are navigation targets only in P8, per Out of scope), storage meter ("Browser storage X GB / Y GB · Results live in this browser only" via `QuotaPort`), header WebGPU/WASM pill.
- **P8A — Upload page.** Single-file drop zone + browse fallback (drag-and-drop and `<input type="file">`); extra dropped files are ignored (decided in `phase-2-plan.md`'s own P8a check), with a brief inline/toast message naming the single-file limit (exact copy is this task's own call, not fixed elsewhere). File card once a file is loaded: name, format, bit depth, duration, size, "Change file". Two profile cards, Basic (4 stems) and Rock (6 stems, default selected), each with its stem list and `ModelStorePort.getFootprint()`-driven cached/size readout, copy matching the fetched Stitch screen (`Komorebi_Master_Mix_48k.wav` is placeholder-song naming in the mock only — real files use the uploaded name). "Before you start" strip: Engine (WebGPU/WASM, from `OnnxSessionManager`'s own provider detection — reuse its `navigator.gpu` check, do not duplicate provider-selection logic), Estimate (~25 s Rock / ~36 s Basic for a 4-minute song per `spike-s2.md`, scaled by the loaded file's duration), Storage (`QuotaPort.availableBytes()`), Models (cached/download-size readout per profile). Primary action "Separate · {Profile} · {N} stems", disabled until a file is loaded, calls `addToLibrary` then routes to Library on `claimed`/`reused`/`awaiting`/`adopted`; on `quota-refused` shows the forecast/available bytes inline rather than navigating.
- **P8B — Library page.** Dense row list from `CatalogPort.listAll()`. Search box (title/artist substring, client-side). Three sorts only — newest, title, duration — no status/profile filters (`feature-parity.md` line 40). One row per status, copy matching the fetched Stitch screen exactly: `ready` ("Open in mixer" navigates only, no mixer UI built here), `processing` ("Separating · window N of M · P%" from `separate()`'s `onProgress` `{phase:'processing', window, totalWindows}`, "Cancel" button), `preparing`/model-download (`{phase:'preparing', detail}` progress — `SeparateProgressEvent`'s `preparing` phase already carries a fully-formatted `detail` string like `"Preparing Rock: 42%"`, do not reformat it, "Cancel"), `failed` (reason text, "Retry" wired to `retryTrack`, "Remove" wired to `removeTrack`), `interrupted` ("Cancelled before it finished.", "Retry"/"Remove"), `unavailable` ("Stems were removed by the browser. Separate again from the original file.", "Retry"/"Remove"). Cancel shows a confirm dialog first, exact copy from the fetched Stitch screen: "Cancel this separation? This stops the job right now. The work in progress is lost and cannot be resumed." / "Keep running" / "Cancel job". Remove shows a guard (this task's own call for exact copy — a destructive-action confirm, following the same dialog pattern as cancel). Call `runStartupSweeps()` once on Library mount (recovers unfinished jobs, invalidates stale `ready` rows, removes orphans) before the first `listAll()` read.
- **P8B integration gap discovered during scoping (read directly from source, not assumed): jobs are not actually running yet.** `addToLibrary` (P8A) only creates the catalog row — it never starts inference. Actually running a job is `SeparationQueue.enqueue(trackId, bytes)` (`src/application/separation-queue.ts`), which the UI must own: a live progress event only exists in memory, fed by the queue's own `onProgress(trackId, event)` callback, while a job is actively running in *this* tab. This makes `SeparationQueue` a cross-page, App-level composition concern, not Library-only:
  - `app-dependencies.ts` gains a real `StemStorePort` (`OpfsStemStore`, P5) and `InferencePort` (`OnnxWorkerInference`, P7b) — both already exist and are already tested, just not wired into the UI composition root yet — plus one shared `SeparationQueue` instance built from them.
  - `App.tsx` (already built in P8A, this task may touch it) owns a live `Record<trackId, SeparateProgressEvent>` fed by the queue's `onProgress`, passed down to both `UploadPage` and the new Library page — this is the one place progress state can live, since it must survive Upload → Library navigation within the tab.
  - `UploadPage.tsx` gets a small necessary follow-up: on a `claimed`/`adopted` decision it must call `queue.enqueue(track.trackId, loaded.bytes)` instead of only showing a static inline message — otherwise "Queued for separation" is cosmetic and no row ever leaves `preparing`. This is a real gap in the already-committed P8A code, not a hypothetical; fix it as part of this task.
  - `SeparationQueue.cancel(trackId)` is what a Library row's "Cancel" button calls (handles both a still-`pending`-in-queue job and the currently-`running` one; returns `false` if the track is neither, per its own doc comment).
- **P8B "Retry" needs the original file back, and the app doesn't keep one.** `docs/decisions/browser-storage.md` §7 states plainly: "Retry after that is only possible if the user still has the original file, since there is no standing handle back to it." `RetryTrackInput.bytes` being optional does not mean retry can skip re-supplying audio to run inference — `retryTrack` alone only flips the catalog row back to `preparing`; something still has to feed real bytes into `SeparationQueue.enqueue`. Design decision for this task (documented here since nothing upstream fixes it): clicking "Retry" on a `failed`/`interrupted`/`unavailable` row opens a small inline re-upload prompt (reuse `DropZone`'s single-file drop/browse component) scoped to that row; once a file is chosen, call `retryTrack(trackId, deps, {bytes})`, and on `ok: true` enqueue those same bytes into the shared `SeparationQueue`. On `retryTrack`'s `identity-owned` refusal, show `ownerTrackId` in the inline error rather than silently failing.

## Out of scope

- Mixer and Export pages themselves, and any `AudioEnginePort` wiring — P9/P10 own them (`phase-2-plan.md`). P8 only navigates *to* a mixer/export destination from a library row ("Open in mixer" / "Export"); it renders nothing there yet beyond a route target. Note: `audio-engine-port.ts`'s own doc comment says "Implemented by P8" — that comment is stale against `phase-2-plan.md`'s P9a ownership; do not let it pull mixer-adapter work into this phase. Flagged separately as a documentation fix, not a P8 task.
- Pixel-perfect visual polish and accessibility audit pass — P11 owns that gate explicitly (`phase-2-plan.md`). P8 implements structure, real copy, real states, and real port wiring against the already-adopted token system and the fetched Stitch reference; it does not chase pixel fidelity against the mockup's exact spacing/typography beyond what the token system already encodes.
- Routing library choice beyond what four static destinations need — no deep-linking, no URL-driven state is required by any decided product behavior; a minimal in-app view switch is sufficient unless a real need surfaces.
- Mobile layouts — desktop-first per `architecture.md`; the Stitch mobile screens are explicitly out of scope for phase 2 (`design-reference.md` section 5, "Not redrawn").

## Constraints and decisions

- Artifacts in English. Conventional Commits, scope `ui`. No AI attribution lines. One commit per task with its tests and the tracker update, matching P7a/P7b's rhythm (source+test commit, then a separate `docs(odd)` tracker commit).
- TDD: strict, on. Runner: `npm test` for pure logic (formatters, sort/search, status-derivation helpers) and component tests that don't need a real browser; `npm run test:browser` for anything rendering real DOM interaction (drop zone drag/drop, file input, dialogs) — matching the existing `App.browser.test.tsx` precedent. Observed RED before every implementation.
- Layering: `src/ui/` may import `application`/`domain` types and call application use cases/ports; it owns its own presentation-only state. Follow container/presentational split and atomic-design grouping (atoms/molecules/organisms/containers or an equivalent this project already leans toward) rather than one flat component per page — confirm the project's actual existing convention (there is none yet beyond `App.tsx`; P8A sets the precedent other UI phases will follow). `npm run lint` enforces the existing architecture boundaries.
- Size heuristic ~400 authored lines per task, advisory, matching `phase-2-plan.md`'s own P8a/P8b split. If either audits out larger (e.g. token-system expansion turns out to be its own coherent unit), split further the same way P7B-05 did — expand with explicit user authorization, never silently.
- RDD: same protocol as P7a/P7b — after each commit, `gentle-ai review assess --base-ref <last acknowledged boundary> --committed-only --json`.
- Checks: `npm run typecheck`, `npm run lint`, `npm test`, `npm run test:browser`, `npm run build`.
- Open, low-risk defaults this task is making explicitly rather than blocking on: (1) extra-dropped-file rejection copy is a brief inline message, not specified elsewhere — reasonable wording, not a hard product commitment; (2) remove-track confirm dialog copy follows the cancel dialog's pattern since no separate copy is fixed anywhere. Both are cheap to change later if the user disagrees.

## Tasks

- [x] **P8A — App shell + Upload page.**
  - Route: delegated direct (writer). Trigger: 2+ non-trivial new files (shell/nav component, drop zone, file card, profile cards, before-you-start strip, token expansion) plus tests.
  - Dependency: none beyond existing P1..P7b ports/use cases.
  - Scope: see Scope section above (shell/nav + tokens.css expansion + everything under "P8A — Upload page").
  - Acceptance: a browser test drops a file and reaches the profile-choice/primary-action state; dropping extra files leaves only the first loaded and shows the ignored-files message; the primary action calls `addToLibrary` with the selected profile and routes correctly per its result `decision`; `quota-refused` is shown inline, not routed away from.
  - Strict TDD: add the browser test first (drop-to-profile-choice flow), record RED, then GREEN/REFACTOR. Run typecheck/lint and the full browser suite.
  - Rollback boundary: revert `src/ui/App.tsx`'s replacement, the new shell/upload components, tokens.css expansion, and this task's evidence; P1..P7b remain fully intact (nothing in `application`/`domain`/`infrastructure` changes).
  - Forecast: ~400 authored lines (phase-2-plan.md's own estimate); expect to exceed given the token-system expansion — do not code-golf tests or copy to fit.

- [x] **P8B — Library page.**
  - Route: delegated direct (writer). Trigger: 2+ non-trivial new files (list container, row component per status, search/sort controls, cancel/remove dialogs, retry re-upload prompt) plus tests; also touches existing `app-dependencies.ts`/`App.tsx`/`UploadPage.tsx` for the `SeparationQueue` wiring described above.
  - Dependency: P8A's shell/nav (Library becomes a real reachable destination) and the `SeparationQueue` integration gap noted above.
  - Scope: see Scope section above, "P8B — Library page" plus the two integration-gap bullets right after it (`SeparationQueue` wiring across Upload/Library, and the retry re-upload prompt).
  - Acceptance: `app-dependencies.ts` wires real `OpfsStemStore`/`OnnxWorkerInference`/`SeparationQueue`; `UploadPage`'s `claimed`/`adopted` path actually enqueues; `runStartupSweeps()` runs once on mount before the first list read; search filters client-side; the three sorts reorder correctly; each of the six row statuses renders its exact designed copy and actions, `processing`/`preparing` rows reflect live queue progress; cancel shows the exact confirm-dialog copy fetched from the Stitch screen and only cancels on "Cancel job" (wired to `SeparationQueue.cancel`); retry re-prompts for the file, then calls `retryTrack`/enqueues, reflecting `identity-owned` refusal; remove calls `removeTrack` and reflects `not-found`/`in-progress`/`unavailable` refusals.
  - Strict TDD: add the browser test first (render all six statuses, drive one cancel-confirm flow and one retry-with-reupload flow end to end through a real enqueued job against the committed synthetic fixture), record RED, then GREEN/REFACTOR. Run typecheck/lint and the full browser suite.
  - Rollback boundary: revert the Library route/components/tests, the `SeparationQueue` wiring in `app-dependencies.ts`/`App.tsx`, and the `UploadPage` enqueue follow-up, plus this task's evidence; P8A's shell and Upload page remain intact and usable on their own (Upload just stops actually enqueueing, same as before this task).
  - Forecast: ~400 authored lines (phase-2-plan.md's own estimate); expect to exceed given the discovered `SeparationQueue`/retry-reupload integration work — do not code-golf tests or copy to fit.

- [x] **P8C — Close P8.**
  - Route: parent-owned commits, assessments, final checks, and Engram mirror update.
  - Evidence: strict-TDD history, exact checks, authored counts, rollback boundaries, commit identities, and native outcomes, matching P7a/P7b's closure entries.

## Acceptance criteria

- [x] The P1 placeholder shell is fully replaced; `src/ui/App.tsx` (or its successor) renders the real four-destination nav and storage meter.
- [x] Upload: single-file drop zone + browse, file card, two profile cards (Basic default-unselected/Rock default-selected per the design), before-you-start strip with real values (no fake DAW telemetry), primary action wired to `addToLibrary`. (Reconciled: this task's own instructions explicitly said not to build real routing to a Library page that doesn't exist yet — all five `addToLibrary` outcomes render an inline status instead of navigating; real Library routing is P8B's job once the destination exists.)
- [x] Library: search, three sorts only, all six row statuses render designed copy/actions, cancel confirm dialog matches fetched copy exactly, retry/remove wired to their use cases including refusal reasons, `runStartupSweeps()` runs on mount before first read (via the session-scoped `StartupSweepGuard`, see evidence below).
- [x] Mixer/Export are reachable as navigation targets only; no `AudioEnginePort` wiring exists in this phase.
- [x] `tokens.css` carries the full Dragon Atelier token set from `design-reference.md` section 1, including the two Rock guitar tints.
- [x] Focused and full checks pass with observed strict-TDD evidence.
- [x] `SeparationQueue` wiring closes the loop: a job enqueued from Upload actually runs end to end and reaches `ready`/a real failure state; Library's Retry genuinely re-runs inference after re-upload, proven by real browser tests against the committed synthetic ONNX fixture, not UI stubs.

## Forecast and delivery

Forecast: P8A ~400+ lines (token expansion likely pushes it over); P8B ~400 lines; P8C closure only. Two independently reviewable work units matching `phase-2-plan.md`'s own P8a/P8b split. Expect a chained sequence of pull requests at closure, same as P7a/P7b. The 400-line figure is advisory, not a target to hit by cutting tests, copy, or states.

## Applicable checks

- P8A focused: its own `*.browser.test.tsx` (drop-to-primary-action flow) plus any pure-logic unit tests (formatters, provider/estimate derivation).
- P8B focused: its own `*.browser.test.tsx` (all six statuses, one cancel-confirm flow) plus pure-logic unit tests (search/sort).
- Closure: `npm test`, `npm run lint`, `npm run typecheck`, `npm run test:browser`, and `npm run build`.
- Review focus: exact port/use-case contract adherence (`addToLibrary`'s five `decision` outcomes, `separate()`'s progress phases, `retryTrack`/`removeTrack`'s refusal reasons), no fabricated DAW telemetry or filters the product already rejected (section 3 of `design-reference.md`), accessibility basics (labelled regions, dialog focus, keyboard-operable primary flows) even though full a11y polish is P11's job.

## Progress / evidence

- 2026-09-22 — Feature document created on `feat/p7b-onnx-worker` (has P1..P7b). Design reference gathered directly rather than trusted from memory: fetched both redrawn Stitch screens (Upload `1d2398e695504d59be8edca762a85bcd`, Library `45c441aa237c4c448a7fa020c5320400`) via the Stitch MCP and read their generated HTML structure/copy in full — the exact strings recorded in Scope above (drop zone text, file card fields, profile card copy, before-you-start labels, primary button format, all six library row statuses, and the cancel-confirm dialog copy) come from that read, not from `design-reference.md`'s summary alone. Explored existing state first (`src/ui/` is still the P1 placeholder; `src/application/ports/` has 8 working ports; `addToLibrary`/`separate`/`retryTrack`/`removeTrack`/`runStartupSweeps` are implemented, not stubs) via a delegated read-only mapping pass before writing this document.
- 2026-09-22 — Four open items surfaced during exploration were resolved without blocking on the user: (1) cancellation dialog copy — resolved, the fetched Library screen has the exact designed copy, recorded in Scope; (2) multi-file-drop rejection copy — no fixed copy exists anywhere, left as this task's own low-risk call, documented in Constraints; (3) `audio-engine-port.ts`'s stale "Implemented by P8" doc comment — resolved by treating `phase-2-plan.md` as authoritative (P9 owns it), flagged as a separate tiny doc-fix follow-up, not a P8 task; (4) pixel-fidelity vs. functional-first — resolved by Constraints' explicit statement that P8 implements structure/copy/states/wiring and P11 owns visual polish.
- 2026-09-22 — `audio-engine-port.ts` doc-comment fix done inline (mechanical, one file, no research needed) rather than as a separate task: commit `46e56dc` (`docs(application): correct AudioEnginePort phase ownership comment`), corrected "Implemented by P8" to P9.
- 2026-09-22 — P8A implementation (delegated writer). `tokens.css` expanded to the full Dragon Atelier set. New: `src/ui/shell/AppShell.tsx` (+browser test) — header/engine pill, four-destination nav, storage meter; `src/ui/upload/{DropZone,FileCard,ProfileCard,BeforeYouStartStrip,UploadPage}.tsx` (+`UploadPage.browser.test.tsx`) — container/presentational split; pure logic `detect-engine-provider.ts`, `estimate-seconds.ts`, `primary-action-label.ts`, `parse-audio-file-format.ts`, `parse-wav-bit-depth.ts`, `read-audio-file-metadata.ts`; `format/{format-bytes,format-duration}.ts`; `app-dependencies.ts` composition root wiring the real, already-existing adapters (`IndexedDbCatalog`, `CacheApiModelStore`, `NavigatorStorageQuota`, `WebLocksLock`, `WebCryptoHash`) — confirmed via `git status` that nothing under `src/application`/`src/domain`/`src/infrastructure` changed, only `src/ui`/`tests/ui`. `App.tsx`/`App.browser.test.tsx` rewritten as an injectable-dependencies container.
- 2026-09-22 — P8A strict-TDD RED/GREEN log (writer-reported, parent spot-checked): `tests/ui/format.test.ts` and `tests/ui/upload-logic.test.ts` RED (module not found) → GREEN; `UploadPage.browser.test.tsx` (drop-to-primary-action, extra-file rejection, browse fallback, profile switch, claimed-decision success, quota-refused inline — 6 cases) RED (module not found) → GREEN on first implementation; `AppShell.browser.test.tsx` (nav, active state, engine pill, storage meter, children slot) RED → GREEN; `App.browser.test.tsx` RED against the old placeholder → GREEN, one real fix needed mid-cycle (wrap the test's `.click()` in `flushSync` so DOM reconciliation is observed synchronously). Pure-logic tests were grouped two-files-per-concern rather than one-file-per-function — a deliberate consolidation, each group still run RED before GREEN.
- 2026-09-22 — P8A design/port gaps found while wiring, not silently patched (both stayed inside `src/ui`, no `application`/`infrastructure` change made): (1) `QuotaPort` only exposes `availableBytes()` (the delta), not separate `quota`/`usage`, so the storage meter reads "`{available} free`" instead of the mockup's literal "X GB / Y GB" — documented inline in `AppShell.tsx`'s own doc comment; expanding the port is a future call, not made here. (2) Bit depth is only recoverable client-side for WAV (parsed from the RIFF `fmt ` header); `AudioContext.decodeAudioData` always yields decoded float PCM for every other format, so the file card omits bit depth for non-WAV files rather than fabricating a number.
- 2026-09-22 — P8A parent verification: reran `npm test` (29 files / 297 passed) and `npm run test:browser` (17 files / 86 passed) directly, matching the writer's reported counts exactly. Read `app-dependencies.ts`, `App.tsx`, `UploadPage.tsx`, `AppShell.tsx` in full for a correctness/quality spot check: clean container/presentational split, all five `addToLibrary` decisions handled, `quota-refused`/thrown-error paths both surfaced to the user, composition root only references already-existing infrastructure adapters. `npm run typecheck`/`npm run lint`/`npm run build`/`git diff --check` re-confirmed clean.
- 2026-09-22 — P8A authored-line count: 1,157 insertions / 25 deletions across 22 files (`git diff --stat` on the commit). Exceeds the ~400 advisory heuristic as pre-flagged in the tracker itself (token-system expansion was expected to push it over); no tests, copy, or states were cut to fit the number.
- 2026-09-22 — P8A source/test commit `02d4df7` (`feat(ui): add the app shell and Upload page`).
- 2026-09-22 — P8A rollback boundary: revert commit `02d4df7` (and the doc-fix `46e56dc` independently if desired, it is unrelated to the UI code itself); this removes `src/ui/App.tsx`'s replacement, `src/ui/shell/`, `src/ui/upload/`, `src/ui/format/`, `src/ui/app-dependencies.ts`, `tests/ui/`, and the `tokens.css` expansion, restoring the P1 placeholder shell exactly. Nothing in `application`/`domain`/`infrastructure` is touched by this rollback.
- 2026-09-22 — P8A acceptance-criteria reconciliation: the tracker's own P8A "Acceptance" bullet said the primary action should route "correctly per its result `decision`," but this session's actual delegation instructions explicitly said not to build real routing to a Library page that doesn't exist yet. The writer correctly followed the explicit instruction (inline status for all five decisions); the Acceptance-criteria checklist above was reworded to match reality rather than left contradicting the shipped behavior.

- 2026-09-22 — P8B scoping, before delegating: read `separate.ts`, `separation-queue.ts`, `separation-copy.ts`, `retry-track.ts`, `remove-track.ts`, `catalog-port.ts`, `track.ts`, and `docs/decisions/browser-storage.md` §7 directly rather than assuming. Found two real gaps not visible from the tracker's original Scope wording alone: (1) nothing currently calls `SeparationQueue.enqueue` — P8A's "Queued for separation" message is cosmetic, no job actually runs; fixing this is cross-cutting (`app-dependencies.ts`, `App.tsx`, `UploadPage.tsx`) and folded into P8B rather than left as a silent P8A defect. (2) The app never retains original file bytes (`browser-storage.md` §7: "there is no standing handle back to it"), so Library's "Retry" cannot be a bare button calling `retryTrack` alone — it must re-prompt for the file first. Both are now recorded in Scope/Tasks above with the exact design decision made for each, before the writer starts, matching this project's own precedent (P7B-05's own prerequisite audit) of auditing before implementing rather than discovering these mid-review.
- 2026-09-22 — P8B implementation (delegated writer). New: `src/ui/library/{LibraryPage,TrackRow,SearchSortControls,CancelConfirmDialog,RemoveConfirmDialog,RetryReuploadPrompt}.tsx` (+`LibraryPage.browser.test.tsx`), `filter-sort-tracks.ts`, `track-row-copy.ts`, `remove-refusal-copy.ts`, `library-copy.ts`; `tests/ui/library-logic.test.ts`; `tests/fakes/build-fake-app-dependencies.ts`. Modified: `app-dependencies.ts` (added real `OpfsStemStore`/`OnnxWorkerInference`, one shared `SeparationQueue`, a `ProgressHub` pub-sub so `App.tsx` can own live per-track progress without the queue depending on React, and a `StartupSweepGuard`), `App.tsx` (threads the queue/progress/guard through), `UploadPage.tsx` (`claimed`/`adopted` now calls `queue.enqueue`; `reused`/`awaiting` correctly skip it since those jobs are already owned elsewhere).
- 2026-09-22 — P8B design gap found and fixed during implementation (stayed inside `src/ui`, no application-layer change): calling `runStartupSweeps()` on every `LibraryPage` mount could wrongly interrupt a job genuinely running in the same tab (`recoverUnfinished` can't distinguish "orphaned by a crash" from "actively running right now" — both look like a `preparing`/`processing` row). Fixed with `StartupSweepGuard`, a small class that runs the sweep exactly once per app session, kicked off synchronously inside `createAppDependencies()` itself — which the JS event loop guarantees completes before any user interaction (a click handler) can possibly fire — with `LibraryPage`'s own mount simply awaiting that same shared promise. `LibraryPage` also polls `catalog.listAll()` every 200ms while mounted (documented inline) so a live job's status transitions are observed without a dedicated catalog-change-notification port, which doesn't exist yet.
- 2026-09-22 — P8B strict-TDD log (writer-reported, parent spot-checked): pure logic (`filter-sort-tracks`, `track-row-copy`, `remove-refusal-copy`) was test-first with observed RED (`Cannot find module`) before GREEN. Components/wiring were implemented first given the scope, then the browser-test suite run against real code produced genuine RED for three real bugs before GREEN: the `StartupSweepGuard` race above; a test-fixture bug (a `ready` fixture correctly flipped to `unavailable` by `validateReady` because the test hadn't seeded matching stem-store entries, fixed in the test, not the source); and a React controlled-input test bug (`.value =` doesn't notify React's internal tracker, fixed with the standard native-setter workaround). Both integration gaps from the scoping note above are proven closed end to end, not stubbed: `UploadPage.browser.test.tsx` drives drop → primary action → a real `SeparationQueue`/`separate()` run against `FakeInference` → track reaches `ready` with every lane key written; `LibraryPage.browser.test.tsx` drives a `failed` row → Retry → choose file → real re-run → `ready`.
- 2026-09-22 — P8B known UI-reachability limitation, not a bug: `removeTrack`'s `not-found` and `in-progress` refusal reasons can't be triggered through the built UI (Remove only renders for `failed`/`interrupted`/`unavailable` rows, structurally guarding `in-progress`; `not-found` needs a delete-race). Both branches are implemented with their copy unit-tested directly (`removeRefusalMessage`); only the `unavailable` refusal (stem-store delete failure) is exercised through a full browser-test flow.
- 2026-09-22 — P8B parent verification: reran `npm test` (30 files / 306 passed) and `npm run test:browser` (18 files / 99 passed) directly, matching the writer's reported counts. `npm run typecheck`/`npm run lint`/`npm run build`/`git diff --check` re-confirmed clean. Read `app-dependencies.ts`, `UploadPage.tsx`'s enqueue fix, and `LibraryPage.tsx` in full: composition-root wiring is sound, the `StartupSweepGuard` race-avoidance reasoning holds (event-loop ordering genuinely guarantees the sweep starts before any possible user interaction), cancel/retry/remove all wired to the correct use cases with refusal handling. Live-verified in the browser preview: Library renders its empty state and search/sort controls correctly, Mixer/Export render their "Coming soon." placeholders, no console errors across all four destinations. Did not drive a real file drop through the ad hoc preview session (no file-upload tool exists for the built-in browser pane); relied on the automated browser-test suite's real Worker/OPFS/synthetic-fixture proof for the actual enqueue-to-ready path instead, which is the stronger check.
- 2026-09-22 — P8B authored-line count: 1,285 insertions / 66 deletions across 18 files (`git diff --stat` on the commit). Exceeds the ~400 advisory heuristic as pre-flagged (the discovered `SeparationQueue`/retry-reupload integration work was expected to push it over); no tests, copy, or states were cut to fit the number.
- 2026-09-22 — P8B source/test commit `e397509` (`feat(ui): add the Library page and wire the separation queue end to end`).
- 2026-09-22 — P8B rollback boundary: revert commit `e397509`; this removes `src/ui/library/`, `tests/ui/library-logic.test.ts`, `tests/fakes/build-fake-app-dependencies.ts`, and reverts `app-dependencies.ts`/`App.tsx`/`UploadPage.tsx` to their P8A state (Upload stops actually enqueueing, same as before this task). P8A's shell and Upload page remain intact and independently usable on their own.

- 2026-09-22 — P8C final verification, parent-run at HEAD (`c61a3e9`): `npm run typecheck` clean; `npm run lint` clean; `npm test` → 30 files / 306 tests; `npm run test:browser` → 18 files / 99 tests; `npm run build` succeeded (67 modules transformed, includes the real Worker chunk and WASM asset now that `OnnxWorkerInference` is wired into the UI); `git diff --check` clean.
- 2026-09-22 — P8C native review attempt: `gentle-ai review assess --base-ref 4946381 --committed-only --json` on the full P8A+P8B range (38 files, 2525 lines, since P7b's closure) classified it medium risk, `review_due: true` (`slice_budget_reached`). User granted candidate-scoped consent. Native lineage `review-8a80e28e06586c29`, lens `review-reliability`, ran `capture-result` and closed `correction_required` ("candidate-caused severe findings require one bounded correction") — but the capture's own returned `status_continuation`, run unchanged (and reproduced twice), classified the just-minted authority `corrupted_or_unverifiable_authority` (terminal stop; `repair.status: unsupported`, all counts zero). No finding body was ever exposed at any step, so no corroborated finding exists to act on — this is a tooling failure, not a review outcome.
- 2026-09-22 — P8C defect handoff: this exact failure shape (a `correction_required` capture's own `status_continuation` immediately classifying `corrupted_or_unverifiable_authority`, finding bodies never exposed) matches the canonical open upstream tracker `Gentleman-Programming/gentle-ai#4571`, reproduced there across builds 2.8.2 through 3.5.0 (newer than this session's installed 3.4.0) with no published fix in either evidence channel. With the user's explicit consent, added one sanitized occurrence comment to that issue (build/OS/contract/candidate-shape evidence only, no repository name, paths, or content) rather than opening a duplicate. Then ran the exact captured `--consent declined` invocation for this candidate (validated `action: "declined"`, `consent: "declined_this_candidate"`) and re-entered through native selectorless STATUS, which confirmed the review store itself is otherwise healthy (a fresh, unrelated target starts normally) — the corruption was specific to this one lineage/capture, not systemic. No review authority or receipt exists for this candidate; delivery follows ordinary repository policy, same precedent as P7A-02B's `lens_context_budget_exceeded` stop.
- 2026-09-22 — P8C commit identities across the whole feature: `46e56dc` (`AudioEnginePort` doc fix), `0e4fb62` (tracker opened), `02d4df7` (P8A source/tests), `3eb6fcd` (dev preview launch config), `7586415` (P8A tracker), `7b83dcb` (P8B scope expansion), `e397509` (P8B source/tests), `c61a3e9` (P8B tracker). Native outcome: attempted, blocked by an upstream tooling defect (reported, see above), never admitted — not a finding against this candidate.
- 2026-09-22 — P8C rollback boundaries, cumulative: reverting `02d4df7` removes the entire app shell/Upload page and restores the P1 placeholder; reverting `e397509` on top of that removes only the Library page and the `SeparationQueue` wiring, leaving Upload's own primary action back to a non-enqueuing cosmetic state (its P8A behavior); the two are independently revertible in that order. `46e56dc` (doc fix) and `3eb6fcd` (dev config) are unrelated to the UI code and revert independently of either.

## Next step

P8 is closed. Continuing work moves to P9 (mixer/player UI, `AudioEnginePort` wiring — the port already exists, unimplemented) per `phase-2-plan.md`, or to publishing this phase's pull requests against the existing chain (`main` → … → P7b → P8), a separate user-owned decision. The upstream `gentle-ai#4571` defect has no action pending on this side; a published fix, if one lands, would let a future phase's native review actually complete rather than skip.
