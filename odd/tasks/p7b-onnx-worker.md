# P7b — ONNX Worker: session, provider selection, windowed inference, cancellation

Status: opened 2026-09-21. Delivery strategy: `ask-on-risk`; chain strategy: `feature-branch-chain` (continuing P7a's convention). Developed locally on `feat/p7a-windowing` (has P5, P6 and P7a); publication base decided at closure, same reasoning as P7a (branch directly off whichever of P4/P5/P6/P7a this code actually imports from).

## Objective

Implement `InferencePort` for real: a dedicated Worker hosting `onnxruntime-web`, selecting the WebGPU execution provider with a WASM fallback, running the fixed-window inference loop from P7a over model bytes obtained from `ModelStorePort` (P6), and writing each stem lane through `StemStorePort` (P5). `terminate()` cancels cleanly with no partial `ready` row.

## Problem / why

Every earlier phase built the pieces this needs: P5 stores stems, P6 caches verified model weights, P7a provides deterministic windowing and the Basic-only spectral transform. Nothing yet runs the actual neural network. P7b is the phase that makes separation real, matching spike S2's proven runtime choice (`docs/decisions/spike-s2.md`: ONNX Runtime Web, WebGPU first, WASM fallback, ~10x realtime on the reference machine) and the two pinned exports (`docs/decisions/weight-mirrors.md`: `Ghilda/htdemucs-onnx` for Basic, `kramp/htdemucs-6s-webgpu-onnx` for Rock).

## Scope

- Add `onnxruntime-web` (pin `1.30.0`, the version S2 validated) as a real npm dependency, bundled through Vite — not a CDN import, unlike the S2 spike.
- Worker message protocol as pure TS types shared by both sides: job envelope (trackId, profile, resultKey, source bytes), progress messages (window/totalWindows), a result message (written lane keys), an error message, and a cancel message.
- `OnnxSessionManager`: creates an `ort.InferenceSession` from model bytes with the WebGPU execution provider, falling back to WASM on session-creation failure (matching S2's finding that `htdemucs.onnx`'s un-folded `ConstantOfShape` nodes ran fine on WebGPU in Chromium, but the fallback path must still exist for browsers without WebGPU).
- Two `PlanarWindowProcessor` implementations (P7a's callback shape) that each session type plugs into `processPlanarWindows`:
  - Rock: single `mix` input, single `stems` output, both `[1, 2|6, samples]`-shaped — self-contained, no external DSP.
  - Basic: `buildCacInput` (P7a) before the session call, `mix`+`mag` inputs, `freq`+`time` outputs, `decodeCacOutputToWaveforms` + `combineFrequencyAndTimeBranches` (P7a) after.
- `OnnxWorkerInference` implementing `InferencePort`: spawns the Worker, calls `ModelStorePort.ensure()` for the job's profile, sends the job, relays progress, on completion writes each stem lane through `StemStorePort.writeLane` (P5's float32 WAV codec) and resolves with the written lane keys; `terminate()` calls `Worker.terminate()` and rejects the pending result with `InferenceCancelled`, per the existing `InferencePort` contract.
- **Test fixture strategy (technical decision, not a product one):** the real pinned models are 174–285 MB and cannot be part of an automated test suite. Automated browser tests exercise the Worker's own logic — session creation, provider fallback, message protocol, progress, cancellation, `StemStorePort` writes — against **synthetic ONNX models** with the real production I/O shapes (`mix [1,2,343980]`, Rock's `stems [1,6,2,343980]`, Basic's `mag [1,4,2048,336]` / `freq [1,4,4,2048,336]` / `time [1,4,2,343980]`) but near-zero weights (a single deterministic op, e.g. scale-by-constant), so runtime behaviour is exercised end-to-end while the fixture stays small enough to commit. Generate them once, read-only, with Python's `onnx` package (same pattern as the P7a golden fixtures) and record the exact script. Numerical fidelity against the real pinned models and the S2 reference numbers is a **manual** verification step at closure (P7B-06), matching `docs/decisions/phase-2-plan.md`'s own note for this row ("Manual check on the reference song against S2's numbers") — never an automated CI assertion.

## Out of scope

- The mixer's audio decoding into `AudioBuffer` and playback (P9).
- UI (P8): progress rendering, upload flow.
- Quota pre-flight before starting a job (already P3a's `addToLibrary`); this phase only runs once a job exists.
- Changing P5/P6/P7a code except to import from it; report anything that looks wrong instead of patching around it.
- Cross-origin isolation / multithreaded WASM (spike S1 territory); this phase runs WASM single-threaded, matching the S2 baseline.

## Constraints and decisions

- Artifacts in English. Conventional Commits, scope `inference` or `infrastructure` matching the touched layer, no AI attribution lines. One commit per task with its tests and the tracker update, matching P7a's established rhythm (source+test commit, then a separate `docs(odd)` tracker commit).
- TDD: strict, on. Runner: `npm test` for pure-TS protocol/session-selection logic where it can be isolated from the Worker boundary; `npm run test:browser` for anything touching a real Worker, `onnxruntime-web`, or `StemStorePort`/`ModelStorePort` adapters. Observed RED before every implementation, recorded per task exactly as P7a's entries.
- Layering: `src/infrastructure/onnx-worker/` implements `InferencePort` and imports `domain`/`application` types plus its own P7a modules; never imports `ui`. `npm run lint` enforces it.
- Size heuristic ~400 authored lines per task, advisory; this row was flagged in the plan as likely to exceed ~400 total, hence the six-task split below.
- RDD: the same protocol as P7a — after each commit, `gentle-ai review assess --base-ref <last acknowledged boundary> --committed-only --json`; if `review_due` and a large binary/data fixture risks `lens_context_budget_exceeded` again, keep fixture-heavy commits in their own small slice separated from code-heavy commits, and record the same terminal-limitation note if it recurs rather than retrying indefinitely.
- Checks: `npm run typecheck`, `npm run lint`, `npm test`, `npm run test:browser`, `npm run build`.

## Tasks

- [x] **P7B-01 — Dependency, protocol types, and synthetic test-fixture models.**
  - Route: delegated direct. Trigger: new dependency, shared protocol types, and binary test fixtures.
  - Add `onnxruntime-web@1.30.0` to `package.json`; confirm it builds through Vite (a worker entry importing it, even a trivial one, must build and the bundle must be servable).
  - Define `WorkerJobMessage`, `WorkerProgressMessage`, `WorkerResultMessage`, `WorkerErrorMessage` (or an equivalent discriminated union) in `src/infrastructure/onnx-worker/protocol.ts`.
  - Generate two synthetic `.onnx` fixtures (Rock-shaped, Basic-shaped) with the real I/O shapes and trivial deterministic weights, via a documented read-only Python snippet; commit them under `tests/infrastructure/onnx-worker/fixtures/`.
  - Checks: `npm run build` succeeds with the new dependency; a browser test loads each synthetic fixture into an `ort.InferenceSession` on the WASM provider and confirms the expected output shape and a known deterministic value.
- [x] **P7B-02 — `OnnxSessionManager`: provider selection and fallback.**
  - Route: delegated direct. Trigger: new adapter with real WebGPU/WASM branching logic and browser tests.
  - RED/GREEN: session creation prefers WebGPU when `navigator.gpu` is present and session creation succeeds; falls back to WASM on any WebGPU session-creation failure; a typed error when both fail.
  - Checks: browser tests against the P7B-01 fixtures on Chromium (WebGPU may or may not be available in the test browser; assert the fallback path works either way, and assert WebGPU is used when available).
- [x] **P7B-03 — Rock inference path.**
  - Route: delegated direct. Trigger: new `PlanarWindowProcessor` implementation plus browser tests running real inference on the synthetic fixture.
  - RED/GREEN: a `PlanarWindowProcessor` that runs the Rock-shaped session per window, called through P7a's `processPlanarWindows` end to end on a short synthetic signal, producing the documented deterministic output.
  - Checks: browser test asserts per-window progress is monotonic and the final accumulated output matches the fixture's known deterministic transform.
- [x] **P7B-04 — Basic inference path.**
  - Route: delegated direct. Trigger: wires three P7a modules (`buildCacInput`, session run, `decodeCacOutputToWaveforms`/`combineFrequencyAndTimeBranches`) together; substantial enough for its own slice.
  - RED/GREEN: a `PlanarWindowProcessor` that builds the CAC input, runs the Basic-shaped session, decodes and combines branches, on the P7B-01 Basic fixture.
  - Checks: browser test on a short synthetic signal, same progress/determinism assertions as P7B-03.
- [x] **P7B-05A — Verified model-byte retrieval after `ModelStorePort.ensure()`.**
  - Route: delegated direct. Trigger: one application-port extension, its Cache API implementation, fake parity, and browser tests across a trust boundary.
  - Dependency: P7B-01's pinned manifest and P6's verified Cache API store.
  - Scope: extend `ModelStorePort` with a byte-retrieval operation for the selected profile and implement it in `CacheApiModelStore`. The operation reads only the active revision named by the injected/pinned manifest, returns a detached `Uint8Array`, repeats size and SHA-256 verification before exposing bytes, and deletes/refuses a missing or corrupt entry. `ensure()` remains responsible for acquisition and progress; retrieval never downloads or silently repairs.
  - Acceptance: after `ensure()`, retrieval returns bytes identical to the verified cache entry; unknown, missing, size-mismatched, and digest-mismatched entries fail closed with typed errors; corrupt entries are removed; `InMemoryModelStore` supports deterministic bytes for application tests.
  - Strict TDD: add the Cache API browser cases first; run `npm run test:browser -- src/infrastructure/cache-api/cache-api-model-store.browser.test.ts` and record RED, then GREEN/REFACTOR. Run affected Node tests for the fake plus typecheck/lint.
  - Rollback boundary: revert only the new `ModelStorePort` byte method, `CacheApiModelStore` implementation/errors, fake support, focused tests, and this task's evidence; existing `ensure()` acquisition semantics remain intact.
  - Forecast: ~100–180 authored lines. Keep as one model-storage work-unit commit with its tests.

- [x] **P7B-05B — Browser audio decoding seam for inference PCM.**
  - Route: delegated direct. Trigger: browser-only Web Audio boundary, transferable buffer ownership, and deterministic browser tests.
  - Dependency: none beyond the existing `InferenceJob.source` byte contract; may proceed after P7B-05A or in isolation, but P7B-05D requires both.
  - Scope: add an infrastructure-local decoder abstraction used by `OnnxWorkerInference`. The production implementation copies the uploaded bytes into an owned `ArrayBuffer`, decodes with an `AudioContext` created at the models' 44,100 Hz rate, clones two non-empty equal-length channels into owned planar `Float32Array`s, returns the sample rate, and closes the context. This is the main-thread decoding exception already fixed by `docs/decisions/architecture.md`; it is not the P9 mixer engine.
  - Acceptance: a committed/test-generated stereo WAV decodes to two aligned 44,100 Hz planar channels with known samples; a source decoded at another native rate is resampled by the 44,100 Hz context; decode failure, empty audio, and non-stereo input fail explicitly rather than inventing an undocumented mono duplication or surround downmix policy. Returned channel buffers are safe to transfer to the Worker.
  - Strict TDD: add the focused browser test first; run `npm run test:browser -- src/infrastructure/onnx-worker/audio-decoder.browser.test.ts` and record RED, then GREEN/REFACTOR. Run typecheck/lint and the full browser suite.
  - Rollback boundary: remove the decoder module/test and this task's evidence; `InferenceJob.source` remains the unchanged raw-byte contract.
  - Forecast: ~120–200 authored lines. Keep Web Audio lifecycle and channel-policy tests in the same work unit.

- [x] **P7B-05C — Deterministic raw-stem to profile-lane assembly.**
  - Route: delegated direct. Trigger: new profile-driven audio transformation with desktop-parity math and multi-lane validation.
  - Dependency: P7B-03/P7B-04 inference result shapes and the existing `StemProfile` metadata.
  - Scope: add a pure TypeScript assembler from named raw model outputs to the profile's final lanes. Basic reorders `BASIC_STEM_LANES` (`drums, bass, other, vocals`) into profile lane order. Rock reorders `ROCK_STEM_LANES` and ports the desktop's deterministic `center-sides-v1`: for stereo guitar, `mid=(L+R)/2`, `side=(L-R)/2`, center=`[mid,mid]`, sides=`[side,-side]`; its residual lane is the sample-wise sum of `piano + other`. Reject unknown splitters, missing raw outputs, non-stereo data, unequal channel/frame shapes, and non-finite results before encoding or storage.
  - Acceptance: Basic produces exactly `vocals, drums, bass, other`; Rock produces exactly `vocals, drums, bass, guitar_center, guitar_sides, other`; center+sides reconstructs guitar within float32 tolerance; Rock `other` equals piano+other; output lanes follow `profile.lanes` order and never publish raw `guitar` or `piano` keys. Semantics are ported from read-only desktop references `SeparationWorker/engine/stereo_split.py` and `SeparationWorker/demucs_adapter.py::_fold/_assemble_lanes`.
  - Strict TDD: add pure Node tests first; run `npm test -- --run tests/infrastructure/onnx-worker/stem-lane-assembler.test.ts` and record RED, then GREEN/REFACTOR. Run typecheck/lint and the full Node suite.
  - Rollback boundary: remove the assembler module/test and this task's evidence; the raw Basic/Rock inference paths remain intact.
  - Forecast: ~180–280 authored lines. Keep validation, transformations, and their tests together because they form one publication contract.

- [x] **P7B-05D — Real Worker and `OnnxWorkerInference` adapter.**
  - Route: delegated direct. Trigger: dedicated Worker entry, transferable protocol, model/decoder/store integration, progress, and cancellation across the browser boundary.
  - Dependencies: P7B-05A, P7B-05B, and P7B-05C complete.
  - Scope: update the shared protocol so a job transfers verified model bytes, decoded planar PCM, sample rate, profile id, and correlation fields into a dedicated module Worker. The Worker creates the ONNX session through `OnnxSessionManager`, dispatches Basic or Rock, assembles final profile lanes, reports monotonic window progress, and transfers the final planar lane buffers back. `OnnxWorkerInference` calls `ModelStorePort.ensure()` then retrieves bytes, decodes the source, spawns the Worker, encodes every returned lane with `encodeFloat32Wav`, writes through the injected `StemStorePort`, and resolves with `${resultKey}/${laneId}` keys in profile order. A test-only model-byte seam may substitute the committed synthetic fixture; production always uses the verified model store.
  - Cancellation: `terminate()` is idempotent, terminates the Worker, rejects the pending result with `InferenceCancelled`, ignores late messages, and deletes the whole `resultKey` through `StemStorePort.delete()` so a failure or cancellation during storage cannot leave a partially published result. Catalog/`ready` transitions remain owned by `separate()` and are not duplicated here.
  - Acceptance: browser end-to-end tests spawn the real Worker and use a committed synthetic model without a network download; prove the expected final lane count/keys and decoded WAV samples in a real `OpfsStemStore`, monotonic progress, transferable ownership, protocol/session/path integration, and cancellation cleanup. Cover at least Basic end to end; add Rock end to end when the fixture's identical raw stems can assert the P7B-05C residual/split mapping without pretending to measure real-model quality.
  - Strict TDD: add `src/infrastructure/onnx-worker/onnx-worker-inference.browser.test.ts` first; run `npm run test:browser -- src/infrastructure/onnx-worker/onnx-worker-inference.browser.test.ts` and record RED before implementation, then GREEN/REFACTOR. Run `npm run typecheck`, `npm run lint`, `npm test`, `npm run test:browser`, `npm run build`, and `git diff --check` before marking complete.
  - Runtime harness: Chromium/Vitest Browser, real module Worker, real ONNX Runtime WASM fallback, synthetic ONNX fixture, and isolated real OPFS root. Record exact scenario/counts.
  - Rollback boundary: remove the Worker entry, adapter, end-to-end browser test, P7B-05D protocol additions, and this task's evidence; P7B-01..04 and prerequisite tasks P7B-05A..C remain independently usable.
  - Forecast: ~300–500 authored lines. The ~400 figure remains advisory; do not remove cancellation/error coverage or split the Worker protocol artificially. Delivery remains `ask-on-risk` / `feature-branch-chain`, with the parent choosing commit/PR slice boundaries from actual authored counts.
- [ ] **P7B-06 — Close P7b.**
  - Route: parent-owned commits, assessments, final checks, and Engram mirror update.
  - Evidence: strict-TDD history, exact checks, authored counts, rollback boundaries, commit identities, and native outcomes, matching P7a's closure entry.
  - Document the manual validation procedure against the real pinned models and the reference song (pointing at `spikes/s2/HARNESS.md` as the closest existing harness and naming what changes for the product Worker), and either run it or explicitly flag it as pending for the user's reference machine.

## Acceptance criteria

- [ ] `onnxruntime-web` builds through Vite; a Worker entry point exists and is servable. (Left unchecked: P7B-01 proved the dependency and bundling half only — `npm run build` succeeds with the dependency present, and the real browser test imports `onnxruntime-web/wasm` and runs a real `InferenceSession` served through Vite. The dedicated Worker entry point itself is P7B-02/P7B-05's deliverable and does not exist yet.)
- [x] WebGPU is preferred and WASM is a real, tested fallback.
- [x] Rock and Basic inference paths each produce correct, deterministic output against their synthetic fixture.
- [ ] Verified model bytes can be retrieved through `ModelStorePort` after `ensure()` without bypassing the port or exposing corrupt cache entries.
- [ ] Raw upload bytes decode to owned stereo 44,100 Hz planar PCM through the documented main-thread Web Audio seam.
- [ ] Basic and Rock raw outputs assemble into the exact profile lane identities; Rock center/sides and residual semantics match the desktop reference.
- [ ] The full `InferencePort` contract holds: progress monotonic, lane keys written on success, `InferenceCancelled` on `terminate()`, no partial/orphaned stem data after cancellation.
- [ ] Focused and full checks pass with observed strict-TDD evidence.
- [ ] Manual validation against the real models is documented, and its outcome (run or explicitly deferred) is recorded.

## Forecast and delivery

Forecast: P7B-01 ~150–250 lines; P7B-02 ~150–250; P7B-03 ~150–250; P7B-04 ~200–300; P7B-05A ~100–180; P7B-05B ~120–200; P7B-05C ~180–280; P7B-05D ~300–500; P7B-06 closure only. The prerequisite expansion makes four independently reviewable work units rather than hiding port, decoding, lane-assembly, and Worker concerns in one oversized commit. Expect a chained sequence of pull requests at closure. The 400-line figure is a per-slice advisory heuristic, not a target to hit by cutting tests, validation, documentation, or cancellation handling.

## Applicable checks

- P7B-01 focused: `npm run build`; a browser test loading each fixture.
- P7B-02 focused: `npm run test:browser -- src/infrastructure/onnx-worker/onnx-session-manager.browser.test.ts` (or equivalent path chosen by the writer).
- P7B-03/04 focused: their own `*.browser.test.ts` files.
- P7B-05A focused: `npm run test:browser -- src/infrastructure/cache-api/cache-api-model-store.browser.test.ts`.
- P7B-05B focused: `npm run test:browser -- src/infrastructure/onnx-worker/audio-decoder.browser.test.ts`.
- P7B-05C focused: `npm test -- --run tests/infrastructure/onnx-worker/stem-lane-assembler.test.ts`.
- P7B-05D focused: `npm run test:browser -- src/infrastructure/onnx-worker/onnx-worker-inference.browser.test.ts`.
- Closure: `npm test`, `npm run lint`, `npm run typecheck`, `npm run test:browser`, and `npm run build`.
- Review focus: provider fallback correctness, cancellation leaving no partial state, exact adherence to the `InferencePort`/`StemStorePort`/`ModelStorePort` contracts already established, and keeping the synthetic-fixture test strategy honest (never silently asserting against the real models' actual audio quality, which stays a manual check).

## Progress / evidence

- 2026-09-21 — Feature document created on `feat/p7a-windowing` (has P5, P6, P7a locally). Publication base to be decided at closure per P7a's own precedent: branch directly off the earliest ancestor this code actually needs, once real imports are known.

- 2026-09-21 — P7B-01 dependency: added `onnxruntime-web@1.30.0` (exact pin, matching S2) to `package.json` dependencies. `npm install` succeeded (16 packages added). `npm run build` verified green with the dependency present before anything imported it (16 modules transformed, unchanged from before the dependency was added), confirming the bare dependency does not break the build.
- 2026-09-21 — P7B-01 fixture generation environment: `D:\cursos\separador-pistas\.venv\Scripts\python.exe` (Python 3.14.6) has numpy 2.5.2 but no `onnx` package, and modifying that shared venv is out of scope. Created a throwaway venv with that same interpreter (`python -m venv <scratch>/onnx-fixture-venv`, scratch under this session's temp scratchpad directory, never under `separador-pistas`) and ran `pip install onnx numpy` (resolved `onnx==1.23.0`, `numpy==2.5.3`, `protobuf==7.36.2`, `ml_dtypes==0.6.0`, `typing_extensions==4.16.0`) into that venv only. Also installed `onnxruntime==1.30.0` into the same throwaway venv (same version as the pinned `onnxruntime-web`) purely as an optional native sanity check, not a project dependency.
- 2026-09-21 — P7B-01 synthetic fixtures: wrote `tests/infrastructure/onnx-worker/fixtures/generate_synthetic_models.py` (161 lines) building two ONNX opset-17 graphs with `onnx.helper`, each graph a Reshape -> Tile -> Mul-by-scalar-constant chain (no learned weights beyond one float32 scalar per output branch), using the real production I/O shapes from `docs/decisions/weight-mirrors.md`: Rock (`mix [1,2,343980]` -> `stems [1,6,2,343980]`, tiled x6, scaled x2) and Basic (`mix [1,2,343980]` + `mag [1,4,2048,336]` -> `time [1,4,2,343980]` scaled x5 + `freq [1,4,4,2048,336]` scaled x3). Ran the script once with the throwaway venv's Python; `onnx.checker.check_model` passed for both graphs, and the script's own optional `onnxruntime` CPU-provider check confirmed the exact expected constant outputs (`atol=1e-6`) before committing. Output file sizes: `rock-synthetic.onnx` 307 bytes, `basic-synthetic.onnx` 586 bytes — both far under the P7a fixture-size lesson (`lens_context_budget_exceeded`), since these carry no bulk numeric data at all.
- 2026-09-21 — P7B-01 strict-TDD RED (`protocol.ts` type guard, the only runtime logic in this file): `npm test -- --run tests/infrastructure/onnx-worker/protocol.test.ts` failed before `src/infrastructure/onnx-worker/protocol.ts` existed (0 tests collected, import error). The rest of `protocol.ts` (`WorkerJobMessage`/`WorkerProgressMessage`/`WorkerResultMessage`/`WorkerErrorMessage`/`WorkerMessage` types) is pure type declarations with no runtime behavior, matching the P3A-01/P7A-01 port-interface precedent — no RED/GREEN cycle applies to the types themselves, only to the `isWorkerMessage` guard function.
- 2026-09-21 — P7B-01 GREEN (`protocol.ts`): the same focused command passed 1 file / 11 tests. Implemented the discriminated union (`job`/`progress`/`result`/`error` on a `kind` tag) plus `isWorkerMessage`, a shallow discriminant-only type guard (documented as intentionally shallow: deep payload validation is deferred to the Worker's own message handler in P7B-05, which owns the real `postMessage` trust boundary).
- 2026-09-21 — P7B-01 browser-test RED/GREEN (`onnx-fixtures.browser.test.ts`, real logic: value-parity numeric assertions against a real `onnxruntime-web` `InferenceSession`): both fixtures already existed and passed onnxruntime's own numpy-side check before this test was written, so there was no natural "fixture missing" RED. Ran the test as first-written and it passed immediately (2/2). To avoid fabricating success without ever observing failure, deliberately mutated the Rock assertion's expected constant from `2` to `999` and reran: RED confirmed (`expected element 0 to be 999, got 2`), proving the assertion is sensitive to the real session output rather than vacuously true. Reverted to the correct expected value (`2`); GREEN confirmed immediately after (2/2, no further changes).
- 2026-09-21 — P7B-01 focused command used for this task's checks: `npm run test:browser -- src/infrastructure/onnx-worker/onnx-fixtures.browser.test.ts` — 1 file / 2 tests, both providers exercised are the WASM execution provider only (`env.wasm.numThreads = 1`, matching the out-of-scope note against multithreaded WASM/cross-origin isolation for this phase); asserts exact output shapes and exact deterministic constant values (`stems` = 2, `freq` = 3, `time` = 5) within `1e-6` tolerance.
- 2026-09-21 — P7B-01 REFACTOR: none needed; both new source files were written directly in their final shape. `npm run typecheck` and `npm run lint` passed on the first pass. Runtime harness: N/A for `protocol.ts` (pure TS, no browser/Worker boundary); the browser harness (Playwright/Chromium via `@vitest/browser-playwright`) is real for `onnx-fixtures.browser.test.ts`, which is the actual browser-boundary proof this task required.
- 2026-09-21 — P7B-01 full verification, parent-run: `npm run typecheck` clean; `npm run lint` clean; `npm test` (node project) 23 files / 248 tests; `npm run test:browser` (browser project) 10 files / 50 tests; `npm run build` succeeded (16 modules, unchanged bundle shape since nothing in `src/ui` imports the new module yet); `git diff --check` clean.
- 2026-09-21 — P7B-01 rollback boundary: remove `src/infrastructure/onnx-worker/protocol.ts`, `src/infrastructure/onnx-worker/onnx-fixtures.browser.test.ts`, `tests/infrastructure/onnx-worker/protocol.test.ts`, `tests/infrastructure/onnx-worker/fixtures/{generate_synthetic_models.py,rock-synthetic.onnx,basic-synthetic.onnx}`, the `onnxruntime-web` dependency in `package.json`/`package-lock.json`, and this task's tracker evidence; P7a (windowing/fft/stft) remains fully intact and no other P7B task has started.
- 2026-09-21 — P7B-01 source/test/fixture commit `fca7fe2` (`feat(inference): add onnxruntime-web, worker protocol types, and synthetic fixtures`) contains 358 authored lines (`package.json` +1, `src/infrastructure/onnx-worker/protocol.ts` 89, `src/infrastructure/onnx-worker/onnx-fixtures.browser.test.ts` 81, `tests/infrastructure/onnx-worker/protocol.test.ts` 26, `tests/infrastructure/onnx-worker/fixtures/generate_synthetic_models.py` 161) plus `package-lock.json` (127 generated lines, not authored logic) and the two binary `.onnx` fixtures (`rock-synthetic.onnx` 307 bytes, `basic-synthetic.onnx` 586 bytes, not authored logic).
- 2026-09-21 — P7B-01 acceptance-criteria note: the first bullet under "Acceptance criteria" is left unchecked on purpose — this task proved the dependency/bundling half (build succeeds, real `InferenceSession` loads through Vite in a browser test) but did not create a Worker entry point file, which belongs to P7B-02 (`OnnxSessionManager`) and P7B-05 (`OnnxWorkerInference`/the actual Worker script).

- 2026-09-21 — P7B-02 design: `OnnxSessionManager` (`src/infrastructure/onnx-worker/onnx-session-manager.ts`) is a class matching `WebLocksLock`'s constructor-injection style (`src/infrastructure/web-locks/web-locks-lock.ts`): an optional `navigatorRef: NavigatorGpuLike` (defaults to the real `navigator`, or `{}` when absent, e.g. the Node test project) plus optional `createWebGpuSession`/`createWasmSession` factories (default to real `ort.InferenceSession.create(..., { executionProviders: [...] })`). `createSession(modelBytes)` tries WebGPU only when `navigatorRef.gpu` is truthy; any failure (missing API, or the WebGPU factory throwing, which covers an adapter-request failure inside onnxruntime-web) falls back to WASM; if WASM also throws, rejects with `OnnxSessionUnavailableError` carrying both `webGpuCause`/`wasmCause`. Imports `onnxruntime-web/webgpu` (confirmed via `node -e` to bundle both the `webgpu` and `wasm` backend strings, same bundle the S2 harness used) and sets `ort.env.wasm.numThreads = 1` at module load, matching P7B-01 and the out-of-scope note against multithreaded WASM. Ported from `spikes/s2/harness.js`'s `detectGpu()` (`!!navigator.gpu`, then `requestAdapter()` in a try/catch) only the presence check; S2's own harness does not auto-fallback (its worker surfaces the exact session-creation error instead of retrying another provider, per `spikes/s2/HARNESS.md`'s "WebGPU concern"), so the fallback behavior itself is new logic for this task, not a port of existing spike behavior.
- 2026-09-21 — P7B-02 strict-TDD RED/GREEN, pure-logic (`tests/infrastructure/onnx-worker/onnx-session-manager.test.ts`, node project, fully injected fakes — no real `onnxruntime-web` call): moved the not-yet-written `onnx-session-manager.ts` aside and ran `npm test -- --run tests/infrastructure/onnx-worker/onnx-session-manager.test.ts`; RED confirmed (`Cannot find module '.../onnx-session-manager'`). Restored the source; same command GREEN, 1 file / 5 tests: WebGPU available and succeeds -> used; WebGPU available but throws -> falls back to WASM (WASM factory never called otherwise); WebGPU unavailable (`navigator.gpu` undefined) -> WASM directly without ever invoking the WebGPU factory; both fail -> `OnnxSessionUnavailableError` with both exact causes; WebGPU-absent case's recorded `webGpuCause` is a descriptive `Error`, not `undefined`.
- 2026-09-21 — P7B-02 strict-TDD RED/GREEN, real runtime (`src/infrastructure/onnx-worker/onnx-session-manager.browser.test.ts`, browser project, real `onnxruntime-web` against the P7B-01 `rock-synthetic.onnx` fixture): the module already existed after the pure-logic cycle above, so — matching P7B-01's own precedent for this exact situation — ran `npm run test:browser -- src/infrastructure/onnx-worker/onnx-session-manager.browser.test.ts` once (passed 2/2 immediately), then deliberately mutated `EXPECTED_STEMS_VALUE` from `2` to `999` and reran: RED confirmed on both tests (`expected element 0 to be 999, got 2`), proving both real-inference assertions are sensitive to actual session output. Reverted to `2`; GREEN confirmed, 2/2, no further changes.
- 2026-09-21 — P7B-02 typecheck fix: `ort.InferenceSession.create`'s typings overload on a bare `Uint8Array` or `ArrayBufferLike` individually, not their union — passing the `OnnxModelBytes = ArrayBuffer | Uint8Array` parameter directly failed `tsc` (`No overload matches this call`). Added a private `toUint8Array` normalizer (`bytes instanceof Uint8Array ? bytes : new Uint8Array(bytes)`) so both default factories call `create` with a concrete `Uint8Array`. `npm run typecheck` clean after; both focused test files rerun and still green (5/5 node, 2/2 browser).
- 2026-09-21 — P7B-02 real-environment finding (answers this task's own applicable-checks question): this repository's Playwright Chromium test browser (`@vitest/browser-playwright`) **does expose `navigator.gpu`**, and creating a real `onnxruntime-web` WebGPU session against the Rock-shaped synthetic fixture **succeeds** and runs correct inference — confirmed by temporarily throwing a diagnostic error out of the "detects this browser's real navigator.gpu" test (`gpuPresent=true provider=webgpu`) before removing the diagnostic. This means the browser test's `expect(provider).toBe('webgpu')` branch is exercised for real in this environment, not just asserted defensively; the `navigatorRef: {}`-forced test proves the WASM fallback path separately and unconditionally, regardless of what this or any other test browser exposes.
- 2026-09-21 — P7B-02 full verification, parent-run: `npm run typecheck` clean; `npm run lint` clean; `npm test` (node project) 24 files / 253 tests; `npm run test:browser` (browser project) 11 files / 52 tests; `npm run build` succeeded (16 modules, same bundle shape as P7B-01 — nothing in `src/ui` imports this module yet).
- 2026-09-21 — P7B-02 rollback boundary: remove `src/infrastructure/onnx-worker/onnx-session-manager.ts`, `src/infrastructure/onnx-worker/onnx-session-manager.browser.test.ts`, `tests/infrastructure/onnx-worker/onnx-session-manager.test.ts`, and this task's tracker evidence; P7B-01 (protocol types, synthetic fixtures) remains fully intact and P7B-03/04/05 have not started.
- 2026-09-21 — P7B-02 source/test commit `1e362da` (`feat(inference): add ONNX session provider selection`) contains 299 authored lines: `src/infrastructure/onnx-worker/onnx-session-manager.ts` 117, `src/infrastructure/onnx-worker/onnx-session-manager.browser.test.ts` 74, `tests/infrastructure/onnx-worker/onnx-session-manager.test.ts` 108.

- 2026-09-21 — P7B-03 design decision (documented per this task's own scoping allowance to deviate from the literal "6 independent processors" suggestion when a cleaner design fits `processPlanarWindows`'s actual signature): Rock's real output is one 4-D tensor `[1, stemCount, channelsPerStem, MODEL_SEGMENT_SAMPLES]`, not 6 independent per-stem tensors. Rather than exposing 6 separate `PlanarWindowProcessor`s that would each need an external cache keyed by window index to share one session run — and would depend on those 6 `processPlanarWindows` calls being driven in lockstep via `Promise.all`, an implicit ordering assumption — `src/infrastructure/onnx-worker/rock-inference.ts`'s `createFlattenedRockProcessor` flattens the 4-D output into `stemCount * channelsPerStem` flat channels per window (`[stem0ch0, stem0ch1, ..., stem5ch0, stem5ch1]`) and drives **one** `processPlanarWindows` call over all of them. Overlap-add is linear and per-channel-independent (each channel's triangular weight and normalization depend only on its own sample position, never on other channels' content), so this is mathematically identical to 6 separate per-stem overlap-add reconstructions, while guaranteeing by construction — not by call ordering — that the session runs exactly once per window. `runRockInference(session, planarMix, onProgress?)` is the public entry point: it runs the flattened processor through `processPlanarWindows` once, then de-interleaves the result back into `stemCount` full-length stereo waveforms. The stem/channel count is never hardcoded to 6 — `createFlattenedRockProcessor` reads `output.dims` off the live session's first `stems` result and validates every subsequent window against that captured shape (enforced for free by `processPlanarWindows`'s own `validateOutput`, which rejects a changed channel count between windows). `ROCK_STEM_LANES` (`drums, bass, other, vocals, guitar, piano`, `docs/decisions/weight-mirrors.md` order) is exported as a documentation aid only, never read for the count itself.
- 2026-09-21 — P7B-03 strict-TDD RED (`src/infrastructure/onnx-worker/rock-inference.browser.test.ts`, browser project, real `onnxruntime-web` against the P7B-01 `rock-synthetic.onnx` fixture): ran `npm run test:browser -- src/infrastructure/onnx-worker/rock-inference.browser.test.ts` before `rock-inference.ts` existed; RED confirmed (Vite import-analysis error, "Failed to resolve import './rock-inference'"). Implemented the source; same command GREEN, 2/2 (`produces 6 full-length stereo stems reconstructing the fixture's known scale-by-2 transform`, `reports monotonically increasing window progress even for a single-window signal`) — first-pass GREEN needed one test-shape correction unrelated to the source's correctness: the "single-window" test's original 343,980-sample (`MODEL_SEGMENT_SAMPLES`) signal actually produces 2 windows (`createWindowOffsets(MODEL_SEGMENT_SAMPLES) === [0, MODEL_WINDOW_STRIDE]`, per `windowing.test.ts`'s own documented case), so the test was corrected to a 10-sample short chunk (`createWindowOffsets`'s genuinely single-window case) before rerunning.
- 2026-09-21 — P7B-03 sensitivity proof (matching P7B-01/P7B-02's precedent): deliberately mutated `EXPECTED_STEM_SCALE` from `2` to `999` and reran; RED confirmed on the scale-reconstruction test (`expected 498.5 to be less than or equal to 0.000001`), proving the assertion is sensitive to real overlap-add-reconstructed session output, not vacuously true. Reverted to `2`; GREEN confirmed immediately after, 2/2, no further changes.
- 2026-09-21 — P7B-03 focused command used for this task's checks: `npm run test:browser -- src/infrastructure/onnx-worker/rock-inference.browser.test.ts` — 1 file / 2 tests, WASM execution provider only (`OnnxSessionManager({ navigatorRef: {} })`, matching the out-of-scope note against relying on any specific browser's real WebGPU availability for a numeric-fidelity assertion); the multi-window test drives a 515,970-sample two-channel signal (3 windows: offsets `0`, `MODEL_WINDOW_STRIDE`, `MODEL_WINDOW_STRIDE * 2`) through all 6 stems and asserts per-window progress `[1,2,3]` plus exact stem/channel shape and `2 * originalMix` reconstruction within `1e-6`; the single-window test asserts progress `[1]` for a 10-sample chunk.
- 2026-09-21 — P7B-03 full verification, parent-run: `npm run typecheck` clean; `npm run lint` clean; `npm test` (node project) 24 files / 253 tests (unchanged from P7B-02 — this task added no Node-runnable logic); `npm run test:browser` (browser project) 12 files / 54 tests (up from 11/52); `npm run build` succeeded (16 modules, same bundle shape — nothing in `src/ui` imports this module yet); `git diff --check` clean.
- 2026-09-21 — P7B-03 rollback boundary: remove `src/infrastructure/onnx-worker/rock-inference.ts`, `src/infrastructure/onnx-worker/rock-inference.browser.test.ts`, and this task's tracker evidence; P7B-01/P7B-02 (protocol types, synthetic fixtures, `OnnxSessionManager`) remain fully intact and P7B-04/05/06 have not started.
- 2026-09-21 — P7B-03 source/test commit `c6ab4a3` (`feat(inference): add the Rock inference path`) contains 243 authored lines: `src/infrastructure/onnx-worker/rock-inference.ts` 150, `src/infrastructure/onnx-worker/rock-inference.browser.test.ts` 93.
- 2026-09-21 — P7B-03 acceptance-criteria note: the "Rock and Basic inference paths each produce correct, deterministic output" bullet under "Acceptance criteria" is left unchecked on purpose — this task closes Rock's half only; it closes once P7B-04 (Basic inference path) also lands.

- 2026-09-21 — P7B-04 design: `createBasicWindowProcessor(session)` validates one fixed stereo model window, builds the raw `mix` tensor plus CAC `mag` via `buildCacInput`, runs the already-open session once, validates the exact `freq [1, stems, 4, 2048, 336]` and `time [1, stems, 2, 343980]` shapes and Float32 data lengths, decodes each source's frequency branch with `decodeCacOutputToWaveforms`, adds the correctly sliced time branch with `combineFrequencyAndTimeBranches`, and returns flattened source/channel lanes. `runBasicInference` drives that processor through one `processPlanarWindows` call, captures the live stem/channel counts, and de-interleaves the reconstructed lanes into immutable per-stem stereo waveforms. This mirrors P7B-03's single-session-run-per-window flattening design and does not hardcode the source count from `BASIC_STEM_LANES`.
- 2026-09-21 — P7B-04 strict-TDD RED was preserved from the interrupted draft: `npm test -- --run tests/infrastructure/onnx-worker/basic-inference.test.ts` failed 8/8 because `createBasicWindowProcessor` was not exported/implemented; `npm run test:browser -- src/infrastructure/onnx-worker/basic-inference.browser.test.ts` failed at that import; `npm run typecheck` failed with TS2305 for the missing export; `npm run lint` passed. No implementation claim is inferred from those failures.
- 2026-09-21 — P7B-04 output-contract correction: missing `freq`/`time` now reports `basic-inference.missing_output`; malformed window input reports `basic-inference.invalid_window_shape`; malformed batch/source/CAC/bin/frame/channel/segment dimensions, non-Float32 output data, or a data-length/dims mismatch reports `basic-inference.invalid_output_shape`; a differing `freq`/`time` source count reports `basic-inference.stem_count_mismatch`. The processor validates output buffers before decoding or slicing them.
- 2026-09-21 — P7B-04 discovered and corrected a pre-existing P7B-01 fixture defect with explicit user authorization: `generate_synthetic_models.py` reshaped `mag` as `[1, CAC, 1, bins, frames]` and tiled axis 2, yielding semantic layout `[batch, CAC, source, bins, frames]`; because both CAC and source counts are 4 and the old test used all-ones input, declared shapes and constants passed while production's `[batch, source, CAC, bins, frames]` contract was transposed. Before the correction, the new non-uniform CAC-channel browser assertion failed (`expected 6, received 3`). The generator now reshapes to `[1, 1, CAC, bins, frames]`, tiles axis 1, regenerates `basic-synthetic.onnx`, and verifies a channel-varying expected tensor. Generation used the isolated throwaway venv `C:\Users\Juan\AppData\Local\Temp\stemslayer-p7b-fixture-venv` (Python 3.14.6; `onnx==1.23.0`, `numpy==2.5.3`, `onnxruntime==1.30.0`); both `onnx.checker` and native CPU inference passed. The shared `D:\cursos\separador-pistas\.venv` was not modified.
- 2026-09-21 — P7B-04 GREEN: `npm test -- --run tests/infrastructure/onnx-worker/basic-inference.test.ts` passed 1 file / 8 tests; `npm run test:browser -- src/infrastructure/onnx-worker/onnx-fixtures.browser.test.ts` passed 1 file / 2 tests after fixture regeneration; `npm run test:browser -- src/infrastructure/onnx-worker/basic-inference.browser.test.ts` passed 1 file / 1 test against the real WASM session. The pure test differentiates every time-branch source scale to catch source slicing errors; the browser test runs `runBasicInference` across two windows and proves monotonic progress, 4x stereo lane grouping, and the fixture's deterministic approximately-8x transform with an explicit tight interior assertion plus the already-documented bounded CAC edge artifact.
- 2026-09-21 — P7B-04 REFACTOR: the browser proof was moved from calling `processPlanarWindows` around the processor directly to the public `runBasicInference` entry point so the same real-runtime test also covers shape capture and stem de-interleaving. No production behavior changed during that refactor; both focused commands remained green.
- 2026-09-21 — P7B-04 full verification: `npm run typecheck` clean; `npm run lint` clean; `npm test` 25 files / 261 tests; `npm run test:browser` 13 files / 55 tests; `npm run build` succeeded (16 modules transformed); `git diff --check` clean. Focused runtime proof: node 1 file / 8 tests and Chromium/WASM 1 file / 1 test; fixture semantic-layout regression proof: Chromium/WASM 1 file / 2 tests.
- 2026-09-21 — P7B-04 rollback boundary: revert source/test commit `60739cb` (`feat(inference): add the Basic inference path`), which removes `src/infrastructure/onnx-worker/basic-inference.ts`, `src/infrastructure/onnx-worker/basic-inference.browser.test.ts`, and `tests/infrastructure/onnx-worker/basic-inference.test.ts` and restores the prior Basic fixture generator, binary, and browser fixture test. P7B-01's Rock fixture plus P7B-02/P7B-03 remain intact.
- 2026-09-21 — P7B-04 authored-line estimate: 617 authored changed lines before this tracker update (576 new Basic source/tests plus 41 additions/deletions for the authorized fixture correction; regenerated binary excluded). This exceeds the ~400 advisory heuristic because the coherent unit includes defensive tensor-contract tests, real-runtime proof, and the discovered fixture regression; no tests, documentation, validation, or readability were removed to code-golf the number. Delivery remains `ask-on-risk` / `feature-branch-chain` for the parent to slice at commit/PR boundaries.
- 2026-09-21 — P7B-04 independent verification and parent spot-check: a fresh verifier reported no blocking, major, or minor findings and reran the focused Node/browser/fixture suites, typecheck, lint, full Node/browser suites, build, and `git diff --check`, all green with the same counts above. The parent reran `npm test -- --run tests/infrastructure/onnx-worker/basic-inference.test.ts`: 1 file / 8 tests passed.
- 2026-09-21 — P7B-04 native review: `gentle-ai review assess --base-ref 1ef2ccb --committed-only --json` classified source/test commit `60739cb` as medium risk with `review_due_reason: slice_budget_reached` (617 authored lines). The user granted candidate-scoped review. The native `review-reliability` lens returned no findings; lineage `review-6510dfc6f47664ec` was approved, acknowledged at revision `sha256:492926b17a23ef3609ca585e880432ada0740e3bd766645dd2d24df47ea26ff2`, and its authority was burned. Delivery remains governed by ordinary repository policy.

- 2026-09-21 — P7B-05 prerequisite audit stopped before implementation and found three contract gaps. First, `ModelStorePort.ensure()` and `CacheApiModelStore` expose cache readiness but no verified byte retrieval, so the Worker cannot receive model bytes without bypassing the port. Second, `InferenceJob.source` is raw uploaded bytes while the existing Worker protocol assumes planar PCM; `docs/decisions/architecture.md` assigns `AudioContext.decodeAudioData` to the main thread, but no decoder seam exists. Third, Rock inference returns raw `drums, bass, other, vocals, guitar, piano`, while `ROCK_PROFILE` publishes `vocals, drums, bass, guitar_center, guitar_sides, other`; no web implementation yet ports the desktop's `center-sides-v1` or `piano + other` residual fold. The protocol also lacks the model-byte payload named by the phase scope. No source or test files were changed during the audit.
- 2026-09-21 — The user explicitly authorized the bounded prerequisite expansion. P7B-05 was therefore reframed as four stable work units: P7B-05A verified model-byte retrieval, P7B-05B browser decoding, P7B-05C final-lane assembly, and P7B-05D the real Worker adapter. P7B-01..04 remain complete and unchanged. This planning update is documentation-only; none of P7B-05A..D is implemented or checked off.
- 2026-09-21 — Verified reference semantics for P7B-05C from the read-only desktop repository: `SeparationWorker/engine/stereo_split.py::split_center_sides` computes stereo mid/side as `mid=(L+R)/2`, `side=(L-R)/2`, then emits center `[mid,mid]` and sides `[side,-side]`, whose sum reconstructs the source; `SeparationWorker/demucs_adapter.py::_fold/_assemble_lanes` sums aligned `piano + other` for the residual and publishes split role lanes in profile order. The browser architecture independently fixes decoding to `AudioContext.decodeAudioData` on the main thread and model input to 44,100 Hz stereo PCM.
- 2026-09-21 — Conservative channel policy for P7B-05B: accept exactly stereo decoded audio and fail explicitly for mono or multichannel input until a product-level upmix/downmix rule is authorized. This avoids silently inventing content or channel weights and does not block the documented stereo baseline. If broader input support becomes a release requirement, it is a separate product decision and task rather than an implicit decoder behavior.
- 2026-09-21 — P7B-05A design: `ModelStorePort.read(profileId)` is deliberately separate from `ensure()`. `ensure()` remains the only acquisition/progress path; `read()` resolves only the active revision from the store's pinned/injected manifest, refuses unknown or missing entries without fetching, validates cached response status/body, repeats exact size and SHA-256 verification, removes unusable/corrupt active entries, and returns `bytes.slice()` so callers own a transferable copy. `ModelNotCachedError` distinguishes an absent ensured entry; `ModelCacheReadError` distinguishes an unusable cached response; existing `ModelSizeMismatchError`/`ModelDigestMismatchError` retain their precise corruption evidence. `InMemoryModelStore.setBytes/read` mirrors owned-copy semantics for downstream application tests without importing infrastructure errors.
- 2026-09-21 — P7B-05A strict-TDD RED: after adding the focused Cache API cases first, `npm run test:browser -- src/infrastructure/cache-api/cache-api-model-store.browser.test.ts` failed before implementation (1 failed file, 0 tests collected) because `cache-api-model-store.ts` did not export `ModelCacheReadError`. This is the expected missing-contract RED; no implementation existed at that point.
- 2026-09-21 — P7B-05A GREEN/REFACTOR: implemented the port, Cache API adapter, and fake support; the same focused browser command passed 1 file / 19 tests. The six new browser cases prove detached verified bytes after `ensure`, unknown/missing fail-closed behavior without network access, size/digest corruption removal, and status/body rejection. `npm test -- --run tests/fakes/in-memory-model-store.test.ts` passed 1 file / 7 tests, including two new owned-copy/missing-byte cases. REFACTOR was a structural review only: acquisition and retrieval stayed separate, existing typed verification errors were reused, and both focused commands remained green without further behavior changes.
- 2026-09-21 — P7B-05A full verification: `npm run typecheck` clean; `npm run lint` clean; `npm test` 25 files / 263 tests; `npm run test:browser` 13 files / 61 tests; `npm run build` succeeded (16 modules transformed); `git diff --check` clean. Runtime harness: Vitest Browser with Playwright/Chromium and the real Cache API; no network or real 174–285 MB model was used.
- 2026-09-21 — P7B-05A authored-line count: 133 authored additions before this tracker update (`model-store-port.ts` 5, `cache-api-model-store.ts` 42, Cache API browser tests 57, in-memory fake 12, fake tests 17). This is below the advisory ~400-line heuristic and remains one coherent model-storage work unit.
- 2026-09-21 — P7B-05A rollback boundary: revert the `ModelStorePort.read` declaration, `CacheApiModelStore.read` plus `ModelNotCachedError`/`ModelCacheReadError`, `InMemoryModelStore.setBytes/read`, their focused tests, and this task's tracker evidence. Existing `getFootprint()`/`ensure()` acquisition, progress, cache activation, and obsolete-revision cleanup remain intact.
- 2026-09-22 — P7B-05A source/test commit `b86fbb2` (`feat(inference): expose verified model bytes`) contains 133 authored additions across the port, Cache API implementation/browser tests, and in-memory fake/tests. An independent verifier found no implementation issues and reran the focused browser suite (19/19), focused fake suite (7/7), typecheck, lint, full Node suite (263/263), full browser suite (61/61), build, and `git diff --check`, all green. The parent spot-check reran the focused fake suite: 7/7 passed.
- 2026-09-22 — P7B-05A native assessment: `gentle-ai review assess --base-ref a4c5794 --committed-only --json` classified commit `b86fbb2` as medium risk with `review_due: false` and `review_due_reason: under_budget` (133 authored lines). It remains pending in the current review slice; no review authority or receipt is claimed.
- 2026-09-22 — P7B-05B design: `WebAudioInferenceDecoder` is an infrastructure-local, main-thread boundary and does not alter `InferenceJob` or P9 playback. It copies the exact `Uint8Array` view into an owned `ArrayBuffer`, creates a short-lived `AudioContext({ sampleRate: 44_100 })`, decodes once, requires exactly two non-empty channels whose lengths match the decoded buffer, clones both channels with `slice()` for safe Worker transfer, returns the decoded 44.1 kHz sample rate plus the planar tuple, and closes the context in `finally` on success and every failure. Typed `AudioDecodeError`, `DecodedAudioEmptyError`, `DecodedAudioChannelCountError`, and `DecodedAudioLengthMismatchError` keep decode, emptiness, channel-policy, and malformed-buffer failures distinct; mono/multichannel remain explicitly unsupported rather than silently up/downmixed.
- 2026-09-22 — P7B-05B strict-TDD RED: after adding `audio-decoder.browser.test.ts` first, `npm run test:browser -- src/infrastructure/onnx-worker/audio-decoder.browser.test.ts` failed (1 failed file / 0 tests) because `./audio-decoder` did not exist. After the first implementation passed 8 tests, an explicit empty-upload case was added and observed RED (1 failed / 8 passed: the empty source incorrectly reached the injected decoder and resolved); the production guard was then added before `decodeAudioData`.
- 2026-09-22 — P7B-05B GREEN/REFACTOR: the final focused command passed 1 file / 9 tests. Real Chromium/Web Audio cases decode a known stereo float32 WAV at 44.1 kHz within `1e-6` and prove a 22.05 kHz WAV is resampled to 44.1 kHz with the expected doubled frame count. Injected-context cases prove exact source-view copying, independently owned channel buffers, raw/decoded emptiness, mono/multichannel rejection, unequal-length rejection, decode-error mapping, and context closure on all paths. REFACTOR only tightened the fake context's TypeScript mock surface; no production behavior changed and the suite remained green.
- 2026-09-22 — P7B-05B full verification: `npm run typecheck` clean; `npm run lint` clean; `npm test` 25 files / 263 tests; `npm run test:browser` 14 files / 70 tests; `npm run build` succeeded (16 modules transformed); `git diff --check` clean. Runtime harness: Vitest Browser with Playwright/Chromium and a real `AudioContext` for decode/resampling, plus injected minimal contexts for deterministic lifecycle/error paths; no committed audio binary was needed because the test builds tiny float32 WAV bytes with the existing codec.
- 2026-09-22 — P7B-05B authored-line count: 258 authored additions before this tracker update (`audio-decoder.ts` 102, `audio-decoder.browser.test.ts` 156). The cohesive browser boundary remains below the advisory ~400-line heuristic.
- 2026-09-22 — P7B-05B rollback boundary: revert source/test commit `be9886a` (`feat(inference): decode stereo audio for inference`), which removes `src/infrastructure/onnx-worker/audio-decoder.ts` and `src/infrastructure/onnx-worker/audio-decoder.browser.test.ts`; remove this task's tracker evidence separately. P7B-05A model retrieval and all P7B-01..04 inference/windowing work remain intact.
- 2026-09-22 — P7B-05B independent verification and parent spot-check: a fresh verifier reported no findings and reran the focused browser suite (9/9), typecheck, lint, full Node suite (263/263), full browser suite (70/70), build, and `git diff --check`, all green. The parent reran `npm run test:browser -- src/infrastructure/onnx-worker/audio-decoder.browser.test.ts`: 1 file / 9 tests passed.
- 2026-09-22 — P7B-05B native assessment: `gentle-ai review assess --base-ref 8193f76 --committed-only --json` classified commit `be9886a` as medium risk with `review_due: false` and `review_due_reason: under_budget` (258 authored lines). It remains pending in the current review slice; no review authority or receipt is claimed.
- 2026-09-22 — P7B-05C design: `assembleStemLanes` accepts named raw planar stems and validates the complete input set, unique names, exactly two channels for every supported raw stem, aligned non-empty channel/frame shapes, and finite samples before returning anything. Basic clones/reorders the raw Demucs order into `profile.lanes`. Rock sums `piano + other`, applies `center-sides-v1` to stereo guitar with `mid=(L+R)/2` and `side=(L-R)/2`, and emits only the six final profile lanes. Returned lane/channel containers are frozen and every `Float32Array` is newly owned; inference inputs are never mutated. `StemLaneAssemblyError` provides stable codes for unknown profile/splitter, raw-set, shape, stereo, and finite-value failures.
- 2026-09-22 — P7B-05C strict-TDD RED: after adding `tests/infrastructure/onnx-worker/stem-lane-assembler.test.ts` first and correcting a test-only parse typo, `npm test -- --run tests/infrastructure/onnx-worker/stem-lane-assembler.test.ts` failed as expected (1 failed file / 0 tests) because `stem-lane-assembler` did not exist. The first production pass then exposed two fixture/validation-order failures (2 failed / 12 passed); correcting the representative Rock fixture shape and prioritizing the explicit split-input stereo error produced GREEN.
- 2026-09-22 — P7B-05C GREEN/REFACTOR: the focused command passed 1 file / 15 tests. Coverage proves exact Basic/Rock lane order, residual sums, no raw guitar/piano publication, owned outputs/non-mutation, representative 97-frame center+sides reconstruction, and typed rejection of unknown profile/splitter, missing/duplicate/unexpected outputs, mono Basic/raw stems, non-stereo guitar, unequal shapes, non-finite inputs, and non-finite transformed residuals. A fresh verifier identified that the initial validation only enforced stereo for Rock's guitar; a focused Basic-mono case observed RED (1 failed / 14 passed), then validation was corrected to require exactly two channels for every supported raw stem while preserving `non_stereo:<rawId>` diagnostics. REFACTOR kept validation separate from pure transforms and retained the final focused result.
- 2026-09-22 — P7B-05C full verification after the stereo-cardinality correction: `npm run typecheck` clean; `npm run lint` clean; `npm test` 26 files / 278 tests; `npm run test:browser` 14 files / 70 tests; `npm run build` succeeded (16 modules transformed); `git diff --check` clean. Runtime harness: pure Vitest Node tests over deterministic planar `Float32Array` fixtures; no browser, ONNX session, filesystem, or model download is applicable to this pure transformation boundary.
- 2026-09-22 — P7B-05C authored-line count: 314 authored additions before this tracker update (`stem-lane-assembler.ts` 169, focused Node test 145). This remains below the advisory ~400-line heuristic and keeps the publication contract with its validation/property-style evidence.
- 2026-09-22 — P7B-05C rollback boundary: revert source/test commit `2eb0f09` (`feat(inference): assemble final stem lanes`), which removes `src/infrastructure/onnx-worker/stem-lane-assembler.ts` and `tests/infrastructure/onnx-worker/stem-lane-assembler.test.ts`; remove this task's tracker evidence separately. P7B-01..04 plus P7B-05A/B remain intact.
- 2026-09-22 — P7B-05C independent verification and parent spot-check: after the stereo-cardinality correction, a fresh verifier reported no remaining findings and reran the focused Node suite (15/15), typecheck, lint, full Node suite (278/278), full browser suite (70/70), build, and `git diff --check`, all green. The parent reran `npm test -- --run tests/infrastructure/onnx-worker/stem-lane-assembler.test.ts`: 1 file / 15 tests passed.
- 2026-09-22 — P7B-05C native assessment: `gentle-ai review assess --base-ref bb98fe7 --committed-only --json` classified commit `2eb0f09` as medium risk with `review_due: false` and `review_due_reason: under_budget` (314 authored lines). It remains pending in the current review slice; no review authority or receipt is claimed.
- 2026-09-22 — P7B-05D protocol/Worker design: the shared protocol now deep-validates transferable verified model bytes, stereo finite planar PCM, sample rate, correlation fields, progress, final lanes, and errors. The dedicated module Worker resolves the fixed profile, creates a session through `OnnxSessionManager`, dispatches the existing Basic/Rock inference path, names raw stems, calls `assembleStemLanes`, enforces monotonic window progress, releases the session, and transfers owned final lane buffers back. Malformed jobs, unsupported profiles/rates, bad output counts, and runtime failures fail closed through correlated error messages.
- 2026-09-22 — P7B-05D adapter design: `OnnxWorkerInference` performs `ensure -> read -> decode -> Worker`, transfers the owned model/PCM buffers, rejects malformed or uncorrelated Worker output, encodes final lanes with `encodeFloat32Wav`, persists them through the injected `StemStorePort`, and resolves exact `${resultKey}/${laneId}` keys in profile order. Cancellation is idempotent, physically terminates the Worker, waits for any active persistence write to stop, deletes the whole result key, rejects with `InferenceCancelled`, and ignores late messages. Any setup, Worker, encoding, or multi-lane persistence failure terminates and deletes the whole result before rejection; catalog state remains untouched.
- 2026-09-22 — P7B-05D strict-TDD RED/GREEN/REFACTOR: after adding `onnx-worker-inference.browser.test.ts` first, `npm run test:browser -- src/infrastructure/onnx-worker/onnx-worker-inference.browser.test.ts` failed (1 failed file / 0 tests) because `./onnx-worker-inference` did not exist. The final focused command passed 1 file / 3 tests. REFACTOR strengthened cancellation against an in-flight persistence race by waiting for persistence to stop before whole-result cleanup, then kept the same focused result. The focused protocol command `npm test -- --run tests/infrastructure/onnx-worker/protocol.test.ts` passed 1 file / 20 tests, including malformed transferable, PCM, progress, lane, correlation, and cancellation-field cases.
- 2026-09-22 — P7B-05D real runtime proof: Vitest Browser/Playwright Chromium spawned the real Vite module Worker, transferred the committed 586-byte Basic synthetic ONNX model from an injected in-memory `ModelStorePort`, decoded a generated 44.1 kHz stereo float32 WAV through real Web Audio, created a real ONNX Runtime session through `OnnxSessionManager`, ran the Basic STFT/window path, and wrote four final WAV lanes to an isolated real `OpfsStemStore`. The test verifies exact profile keys/order, non-empty monotonic progress ending at total, 2,048 output frames, 44.1 kHz stereo, and representative samples at the fixture's deterministic 8x gain. A second real-Worker run terminates after observed progress and proves idempotent cancellation plus whole-result cleanup; a third run injects failure on the second lane write and proves partial OPFS cleanup.
- 2026-09-22 — P7B-05D full verification: `npm run typecheck` clean; `npm run lint` clean; `npm test` 26 files / 287 tests; `npm run test:browser` 15 files / 73 tests; `npm run build` succeeded (16 application modules transformed); `git diff --check` clean. The focused browser suite was rerun after final refactoring and remained 3/3 green; the focused protocol suite remained 20/20 green.
- 2026-09-22 — P7B-05D authored-line count: 652 authored additions plus deletions before this tracker update (`protocol.ts` +100/-60, protocol tests +62/-7, Worker entry 106, adapter 205, browser E2E 112). This exceeds the advisory ~400-line heuristic because the smallest coherent Worker boundary includes the transferable trust contract, real runtime dispatch, storage/cancellation rollback behavior, and their focused proofs; no validation, cleanup coverage, or readability was removed to code-golf the unit. Delivery remains parent-owned `ask-on-risk` / `feature-branch-chain`.
- 2026-09-22 — P7B-05D rollback boundary: revert the protocol and protocol-test changes; remove `src/infrastructure/onnx-worker/onnx-worker-entry.ts`, `src/infrastructure/onnx-worker/onnx-worker-inference.ts`, `src/infrastructure/onnx-worker/onnx-worker-inference.browser.test.ts`, and this task's tracker evidence. P7B-01..04 and prerequisites P7B-05A..C remain independently intact. No commit or native review outcome is recorded here; those remain parent-owned.
- 2026-09-22 — P7B-05D remaining risk: the E2E intentionally proves Basic only; Rock inference and Rock final-lane math are independently covered by P7B-03/P7B-05C but not repeated through the module Worker. The real pinned 174–285 MB production models and reference song remain a P7B-06 manual validation on the user's reference machine. The headless runtime proves the real session-manager path but does not expose the selected provider in the protocol, so this task does not invent a WebGPU-versus-WASM claim beyond P7B-02's existing provider tests.
- 2026-09-22 — P7B-05D coverage follow-up (user-requested, after independent verification found the two Key Learnings were implemented but unproven by any test): added two browser E2E cases to `onnx-worker-inference.browser.test.ts`. First, `cancellation during an in-flight lane write waits for the write before deleting the result` uses a wrapping `StemStorePort` that blocks the first `writeLane` call on a controlled gate and schedules `terminate()` only after the pending `persist()` promise is captured (via `queueMicrotask`, not a raw timer, to avoid the read-before-assignment race); it asserts no `delete` occurs while flushing all pending microtasks through a `setTimeout(0)` barrier, then releases the write and asserts the exact order `write-started, write-resolved, delete-called`. An initial draft used a fixed `setTimeout(50)` race against real OPFS I/O and proved flaky/non-deterministic (passed even when the "wait" logic was removed) before being replaced with this microtask-flush design. Second, `a cleanup failure during cancellation still rejects with InferenceCancelled` injects a `delete` that always throws and asserts the caller still receives `InferenceCancelled` with the original error attached as `cause`.
- 2026-09-22 — P7B-05D coverage follow-up strict-TDD RED (both new cases, since the implementation already existed): mutated `terminate()` in `onnx-worker-inference.ts` to (1) `const persistenceStopped = Promise.resolve()` (ignoring the in-flight `persistence` promise) — RED confirmed 3/3 identical runs (unhandled `InferenceCancelled` rejection, order assertion failing); (2) the reject branch calling `rejectResult(error)` directly instead of wrapping in `InferenceCancelled` — RED confirmed (`expected Error: test.delete_failed to be an instance of InferenceCancelled`). Reverted both mutations; GREEN confirmed 3 consecutive full-file runs (5/5) with no flakiness after the redesign.
- 2026-09-22 — P7B-05D coverage follow-up full verification: `npm run lint` clean (one `prefer-const` fix applied to the new test's local handle variable, matching the file's existing `state.handle` closure pattern used by the idempotent-cancellation test); `npm run typecheck` clean; focused browser file 5/5 (stable across 3 reruns); `npm test` 26 files / 287 tests; `npm run test:browser` 15 files / 75 tests (up from 73); `npm run build` succeeded; `git diff --check` clean (only pre-existing LF/CRLF warnings, no whitespace errors). No source behavior changed — only test coverage was added.
- 2026-09-22 — P7B-05D source/test commit `877378f` (`feat(inference): run separation through a real ONNX Worker`) contains 650 insertions / 67 deletions across `protocol.ts`, `protocol.test.ts`, `onnx-worker-entry.ts`, `onnx-worker-inference.ts`, and `onnx-worker-inference.browser.test.ts` — the original 652-line P7B-05D unit plus the two coverage follow-up tests. Tracker update committed separately as `bef67ac` (`docs(odd): record P7B-05D progress`), matching the established source+test / docs(odd) rhythm. No native review authority has been requested or granted for this commit yet.

- 2026-09-22 — P7B-05D native review: `gentle-ai review assess --base-ref 69e0a9c --committed-only --json` on commit `877378f` (plus the two tracker docs commits) classified the 3-commit slice as medium risk, `review_due: true` (`slice_budget_reached`, 6 files / 733 lines). The user granted candidate-scoped review consent. Native lineage `review-0f5bf8ba022e51ec`, lens `review-reliability`, ran `correction_required`: one CRITICAL finding, `R3-001` — the generic `fail()` failure path (reached from `worker.onerror`, a malformed/uncorrelated message, or non-monotonic progress) called `cleanup()` (whole-result delete) immediately without waiting for an in-flight `persist()` lane write, unlike the sibling `terminate()` path which already guarded against exactly this race; a write could complete and publish its key after the whole result had already been deleted.
- 2026-09-22 — P7B-05D correction for R3-001: `fail()` now awaits `persistence?.catch(() => undefined) ?? Promise.resolve()` before calling `cleanup()`, plus a new `if (settled) return` guard so a concurrently-succeeding `persist()` cannot have its already-resolved, already-returned-to-the-caller result deleted out from under it. Added `tests/infrastructure/onnx-worker/onnx-worker-inference.test.ts` (Node project, fully injected fake `WorkerLike`/`StemStorePort`/decoder — no real Worker/browser needed) proving delete waits for the in-flight write and the original `onerror` failure still wins over a subsequent write failure. Strict-TDD RED confirmed 3/3 consecutive runs on the reverted fix (unhandled `InferenceCancelled`-shaped rejection / thrown assertion), GREEN 3/3 after reapplying. Full verification after: `npm run lint` clean, `npm run typecheck` clean, focused new test 3/3 stable reruns, focused browser E2E 5/5 stable across 2 reruns, `npm test` 27 files / 288 tests, `npm run test:browser` 15 files / 75 tests (unchanged — the correction is Node-only), `npm run build` succeeded, `git diff --check` clean. Committed as `7ef08e0` (`fix(inference): wait for an in-flight lane write before deleting on failure`, 115 authored lines: 2 in `onnx-worker-inference.ts`, 113 new test file).
- 2026-09-22 — P7B-05D native review outcome: after the correction (committed, since declaring the new test file as `--intended-untracked` inside an already-open `correction_required` lineage repeatedly returned `corrected_candidate_unavailable` from `gentle-ai review status` regardless of selection method — committing the file resolved it immediately), the targeted validator (`review.capture-validation`) confirmed `R3-001` fixed and returned `state: approved` with four non-blocking advisory findings for later, separate work (never a reason to reopen this review): `R3-002` (WARNING, `onnx-worker-entry.ts` malformed-job/unsupported-profile/unsupported-rate branches have no dedicated test), `R3-003` (WARNING, the main-thread `worker.onerror`/malformed-message/`result_contract`-mismatch branches in `onnx-worker-inference.ts` have no dedicated test), `R3-004` (SUGGESTION, Rock-profile dispatch through the real module Worker is never exercised end-to-end — matches the already-disclosed remaining-risk note), `R3-005` (SUGGESTION, `protocol.ts`'s `isProgress` guard requires `window > 0` while the window-numbering convention crossing the module boundary is unpinned by any test). Acknowledged via `gentle-ai review acknowledge-approved`; lineage `review-0f5bf8ba022e51ec` authority burned (`consumed_revision sha256:0795c941f8773ab4da98be5757012a0ee3586767b5d0ad052462d1bfbe8ce3e9`). Delivery (push/PR) remains separate, ordinary repository policy, still to be decided.

- 2026-09-22 — Follow-up tasks opened (user-requested) for the four non-blocking advisory findings, as independent background-agent chips rather than new `odd/tasks` entries, since each is self-contained, out-of-scope-for-P7B-05D coverage work: `task_608c3dba` (R3-002, `onnx-worker-entry.ts` trust-boundary branches), `task_fc344d0a` (R3-003, `onnx-worker-inference.ts` postMessage trust boundary), `task_001c8979` (R3-004, Rock profile end-to-end through the real Worker), `task_b45022f0` (R3-005, pin the window-numbering convention). None of these block P7B-06 closure.

## Next step

P7B-05D is implemented, verified, committed, and natively reviewed: `877378f` (source/tests), `bef67ac`/`bfc47f4` (tracker), `7ef08e0` (R3-001 correction, review-acknowledged). Four non-blocking advisory findings (R3-002..R3-005) have follow-up task chips opened (see above), not required for this task's closure. Continue with P7B-06 closure and manual-validation documentation/outcome; no push or PR has been created yet.
